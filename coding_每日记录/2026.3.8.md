# 每日记录

## 📅 基本信息

- **日期**：2026年03月08日
- **星期**：星期日
- **天气**：[晴]

## 📝 今日任务

### 待办事项
- [x] 链表环检测实战（零压力入门周 Day 4）
- [x] LeetCode 141 环形链表（快慢指针法）
- [x] LeetCode 21 合并两个有序链表（迭代法）
- [x] 理解快慢指针的数学原理
- [x] Python实现对比学习
- [x] [可选挑战] LeetCode 142 环形链表II（两阶段法思路）
- [x] LeetCode 70 爬楼梯（动态规划基础）
- [x] LeetCode 283 移动零（双指针）
- [x] LeetCode 15 三数之和（排序+双指针）

## 💡 学习与收获

### 核心收获（3个）
1. 掌握了快慢指针检测环的核心算法（Floyd判圈法）：慢指针每次走一步，快指针每次走两步，如果链表有环则快慢指针必然在环内相遇，时间复杂度O(n)，空间复杂度O(1)
2. 熟练掌握了合并两个有序链表的迭代法：使用哑节点（dummy）简化边界处理，通过比较两个链表当前节点的值，将较小的节点连接到新链表末尾，最后处理剩余部分
3. 理解了快慢指针相遇的数学原理：在环形跑道上，快指针速度是慢指针的2倍，相对速度为1步/单位时间，因此快指针必然追上慢指针，相遇点与环入口的关系为：从头节点和相遇点同时出发同速前进，会在环入口相遇

### 疑问与思考（1个）
- 在环形链表II问题中，为什么从头节点和相遇点同时出发同速前进，会在环入口相遇？虽然记住了结论，但数学推导过程（a = c + (n-1)(b + c)）还是有些抽象

### 感悟与总结（1个）
- 快慢指针是解决链表环问题的经典技巧，通过引入速度差让算法变得直观高效。今天通过"环形跑道"的比喻让抽象的数学原理变得容易理解，这种类比学习方式很有帮助

## 📚 刷题记录

### 今日完成题目
| 题号   | 题目名称       | 难度  | 通过次数 | 完成时间 | 备注               |
| ---- | ---------- | --- | ---- | ---- | ---------------- |
| 141  | 环形链表       | 简单  | 18   | 4分钟前 | 快慢指针法（Floyd判圈）   |
| 21   | 合并两个有序链表   | 简单  | 7    | 3分钟前 | 迭代法（哑节点）         |
| 142  | 环形链表II      | 中等  | 2    | 4分钟前 | 两阶段法（检测环+找环入口）    |
| 70   | 爬楼梯         | 简单  | 16   | 4分钟前 | 动态规划基础            |
| 283  | 移动零         | 简单  | 2    | 5分钟前 | 双指针               |
| 15   | 三数之和        | 中等  | 15   | 3分钟前 | 排序+双指针            |

### 重点知识点
- **快慢指针法（Floyd判圈）**：检测链表是否有环，slow每次走1步，fast每次走2步
- **合并有序链表迭代法**：使用哑节点dummy简化边界处理，比较两个链表当前节点值，连接较小节点
- **两阶段法（环形链表II）**：阶段一检测环并找到相遇点，阶段二从头节点和相遇点同时出发同速前进找到环入口
- **动态规划基础（爬楼梯）**：dp[n] = dp[n-1] + dp[n-2]，空间优化可只用两个变量
- **双指针应用**：移动零（一个指针遍历，一个指针记录非零位置）、三数之和（排序后固定一个数，用双指针找另外两个数）
- **时间复杂度分析**：快慢指针O(n)，合并链表O(n+m)，爬楼梯O(n)，移动零O(n)，三数之和O(n²)

### 代码实现（必做题）

**LeetCode 141 - 环形链表（快慢指针法）**
```cpp
class Solution {
public:
    bool hasCycle(ListNode *head) {
        if (head == nullptr || head->next == nullptr) {
            return false;
        }

        ListNode* slow = head;
        ListNode* fast = head;

        while (fast != nullptr && fast->next != nullptr) {
            slow = slow->next;
            fast = fast->next->next;

            if (slow == fast) {
                return true;
            }
        }

        return false;
    }
};
```

**LeetCode 21 - 合并两个有序链表（迭代法）**
```cpp
class Solution {
public:
    ListNode* mergeTwoLists(ListNode* list1, ListNode* list2) {
        ListNode dummy(0);
        ListNode* curr = &dummy;

        while (list1 != nullptr && list2 != nullptr) {
            if (list1->val <= list2->val) {
                curr->next = list1;
                list1 = list1->next;
            } else {
                curr->next = list2;
                list2 = list2->next;
            }
            curr = curr->next;
        }

        curr->next = (list1 != nullptr) ? list1 : list2;

        return dummy.next;
    }
};
```

### 代码实现（可选挑战题）

**LeetCode 142 - 环形链表II（两阶段法）**
```cpp
class Solution {
public:
    ListNode *detectCycle(ListNode *head) {
        ListNode *slow = head, *fast = head;

        while (fast && fast->next) {
            slow = slow->next;
            fast = fast->next->next;

            if (slow == fast) {
                ListNode *p1 = head, *p2 = slow;

                while (p1 != p2) {
                    p1 = p1->next;
                    p2 = p2->next;
                }

                return p1;
            }
        }

        return nullptr;
    }
};
```

### 代码实现（拓展练习）

**LeetCode 70 - 爬楼梯（动态规划）**
```cpp
class Solution {
public:
    int climbStairs(int n) {
        if (n <= 2) return n;

        int prev2 = 1;
        int prev1 = 2;
        int curr;

        for (int i = 3; i <= n; i++) {
            curr = prev1 + prev2;
            prev2 = prev1;
            prev1 = curr;
        }

        return prev1;
    }
};
```

**LeetCode 283 - 移动零（双指针）**
```cpp
class Solution {
public:
    void moveZeroes(vector<int>& nums) {
        int n = nums.size();
        int left = 0;

        for (int right = 0; right < n; right++) {
            if (nums[right] != 0) {
                swap(nums[left], nums[right]);
                left++;
            }
        }
    }
};
```

**LeetCode 15 - 三数之和（排序+双指针）**
```cpp
class Solution {
public:
    vector<vector<int>> threeSum(vector<int>& nums) {
        vector<vector<int>> result;
        int n = nums.size();

        sort(nums.begin(), nums.end());

        for (int i = 0; i < n - 2; i++) {
            if (i > 0 && nums[i] == nums[i - 1]) continue;
            if (nums[i] > 0) break;

            int left = i + 1;
            int right = n - 1;

            while (left < right) {
                int sum = nums[i] + nums[left] + nums[right];

                if (sum == 0) {
                    result.push_back({nums[i], nums[left], nums[right]});

                    while (left < right && nums[left] == nums[left + 1]) left++;
                    while (left < right && nums[right] == nums[right - 1]) right--;

                    left++;
                    right--;
                } else if (sum < 0) {
                    left++;
                } else {
                    right--;
                }
            }
        }

        return result;
    }
};
```

## 📚 其他记录

### 参考资料
- [LeetCode 141. 环形链表](https://leetcode.cn/problems/linked-list-cycle/)（已完成，通过18次）
- [LeetCode 21. 合并两个有序链表](https://leetcode.cn/problems/merge-two-sorted-lists/)（已完成，通过7次）
- [LeetCode 142. 环形链表II](https://leetcode.cn/problems/linked-list-cycle-ii/)（已完成，通过2次）
- [LeetCode 70. 爬楼梯](https://leetcode.cn/problems/climbing-stairs/)（已完成，通过16次）
- [LeetCode 283. 移动零](https://leetcode.cn/problems/move-zeroes/)（已完成，通过2次）
- [LeetCode 15. 三数之和](https://leetcode.cn/problems/3sum/)（已完成，通过15次）
- 零压力入门周 Day 4：链表环检测实战学习材料
- 算法导论笔记(C++) - Part03_基础数据结构

### 下一步计划
- **今日强度渐进总结**：
  - ✅ 引入可选中等难度题（LeetCode 142）
  - ✅ 提供快慢指针数学原理的简单解释
  - ✅ 保持零压力底线：完成141和21即达标
  - ✅ 必做题提供完整代码模板，可选题仅需理解思路
- **明日（3月9日）**：深度输入日——栈与队列生活化类比（叠盘子/排队+LeetCode 20）
  - 强度渐进预告：明天将减少代码模板依赖，要求独立实现括号匹配逻辑，提供伪代码框架而非完整代码

### 学习建议
- ✅ 重点掌握LeetCode 141的快慢指针实现
- ✅ 熟练LeetCode 21的迭代法合并链表
- ✅ 可选题142只看思路，不要求现在实现
- ✅ 填写执行卡片，记录真实感受（"数学推导难懂"也是有效反馈）

### 零压力原则
- 理论零负担：不推导、不证明
- 练习极简化：每天只做2道必做题
- 代码全供给：提供完整可运行代码
- 弹性无压力：完成即达标，可选挑战不影响进度

---

**记录时间**：2026-03-08