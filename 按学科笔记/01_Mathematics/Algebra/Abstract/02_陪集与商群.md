---
type: note
subject: abstract_algebra
chapter: 02
created: 2026-04-03
updated: 2026-04-25
status: complete
---

# 陪集与商群

## 📌 学习目标

- 理解左陪集、右陪集、正规子群和商群的定义与性质
- 掌握 Lagrange 定理、同态基本定理与同构定理
- 会用陪集分解解释指数、群的阶与商结构

## ✅ 先修

- [[01_群的定义与基本性质]]
- [[../00_Symbols_Conventions|符号与约定总表]]

## 难度分层

- **基础**：陪集、等价关系、Lagrange 定理
- **进阶**：正规子群、商群、同态基本定理
- **拓展**：同构定理、对应定理、商结构分析

## 自测（3问速测）

1. 左陪集和右陪集有什么区别？
2. 为什么正规子群才能构造商群？
3. 同态基本定理说明了什么结构关系？

## 一、陪集

### 1.1 定义

**定义**：设 $H \leq G$，$g \in G$。定义：
- **左陪集**：$gH = \{gh : h \in H\}$
- **右陪集**：$Hg = \{hg : h \in H\}$

**例**：设 $G = \mathbb{Z}$，$H = 3\mathbb{Z}$，则陪集为
$$0 + H, 1 + H, 2 + H$$
即模3的同余类。

### 1.2 陪集的性质

**定理**：设 $H \leq G$，$a, b \in G$，则以下等价：
1. $aH = bH$
2. $b^{-1}a \in H$
3. $a \in bH$
4. $aH \cap bH \neq \emptyset$

**证明**：$(1) \Rightarrow (2)$：若 $aH = bH$，则 $a = ae \in aH = bH$，故 $a = bh$，$b^{-1}a = h \in H$。

$(2) \Rightarrow (3)$：$b^{-1}a = h \in H \Rightarrow a = bh \in bH$。

$(3) \Rightarrow (4)$：显然。

$(4) \Rightarrow (1)$：若 $c \in aH \cap bH$，则 $c = ah_1 = bh_2$，$a = bh_2h_1^{-1}$，故 $aH = bH$。

### 1.3 陪集的等价关系

**定理**：$a \sim b \Leftrightarrow a^{-1}b \in H$ 是等价关系，等价类就是左陪集。

### 1.4 陪集的基数

**定理**：所有陪集与 $H$ 有相同的基数，即 $|gH| = |H|$。

**证明**：映射 $h \mapsto gh$ 是 $H$ 到 $gH$ 的双射。

---

## 二、Lagrange定理

### 2.1 定理陈述

**定理（Lagrange）**：设 $G$ 是有限群，$H \leq G$，则
$$|H| \mid |G|$$
即子群的阶整除群的阶。

### 2.2 证明

**证明**：
1. $G$ 被划分为若干左陪集的并：$G = \bigcup_{i=1}^{k} g_i H$
2. 每个陪集的大小为 $|H|$
3. 因此 $|G| = k \cdot |H|$

**定义**：$k = [G : H]$ 称为 $H$ 在 $G$ 中的**指数**。

### 2.3 推论

**推论1**：设 $|G| = n$，则对任意 $g \in G$，$o(g) \mid n$。

**证明**：$\langle g \rangle$ 是 $G$ 的子群，$|\langle g \rangle| = o(g)$。

**推论2**：$g^{|G|} = e$。

**推论3**：素数阶群是循环群。

### 2.4 逆命题不成立

**注意**：Lagrange定理的逆命题不成立！

**例**：$A_4$（4个元素的交替群）的阶为12，但没有6阶子群。

---

## 三、正规子群

### 3.1 定义

**定义**：设 $H \leq G$。若对任意 $g \in G$，$gH = Hg$，则称 $H$ 是 $G$ 的**正规子群**，记作 $H \trianglelefteq G$。

**等价条件**：
- $gHg^{-1} = H$ 对所有 $g \in G$
- $gHg^{-1} \subseteq H$ 对所有 $g \in G$
- 左陪集等于右陪集

### 3.2 正规子群的例子

**例1**：$\{e\}$ 和 $G$ 是任意群的正规子群（平凡正规子群）

**例2**：$A_n \trianglelefteq S_n$（交替群）

**例3**：$Z(G)$（中心）是正规子群

**例4**：$SL_n(\mathbb{R}) \trianglelefteq GL_n(\mathbb{R})$

### 3.3 正规子群与同态

**定理**：若 $f: G \to H$ 是群同态，则 $\ker f \trianglelefteq G$。

**证明**：设 $k \in \ker f$，$g \in G$，则
$$f(gkg^{-1}) = f(g)f(k)f(g^{-1}) = f(g) \cdot e \cdot f(g)^{-1} = e$$
故 $gkg^{-1} \in \ker f$。

---

## 四、商群

### 4.1 定义

**定义**：设 $N \trianglelefteq G$，$G/N = \{gN : g \in G\}$ 是所有左陪集的集合。定义运算：
$$(aN)(bN) = (ab)N$$

**定理**：$(G/N, \cdot)$ 构成群，称为**商群**。

### 4.2 运算的良定义性

**验证**：设 $aN = a'N$，$bN = b'N$，需证 $(ab)N = (a'b')N$。

由 $aN = a'N$ 知 $a^{-1}a' \in N$。设 $a^{-1}a' = n$，则 $a' = an$。

同理 $b^{-1}b' = m \in N$，$b' = bm$。

$(a'b')N = (an)(bm)N = a(n b) m N = a(b n')mN = (ab)(n'm)N = (ab)N$

（其中用到 $N$ 正规：$Nb = bN$，故 $nb = bn'$ 对某 $n' \in N$）

### 4.3 商群的阶

**定理**：$|G/N| = |G| / |N| = [G : N]$

### 4.4 商群的例子

**例1**：$\mathbb{Z}/n\mathbb{Z} \cong \mathbb{Z}_n$

**例2**：$S_n / A_n \cong \mathbb{Z}_2$

**例3**：$GL_n(\mathbb{R}) / SL_n(\mathbb{R}) \cong \mathbb{R}^*$

**完整例题：$\mathbb{Z}_{12}$ 的商群**

设 $G = \mathbb{Z}_{12}$，取正规子群
$$N = \{0, 4, 8\} = \langle 4 \rangle$$

则 $|G| = 12$，$|N| = 3$，因此
$$|G/N| = \frac{12}{3} = 4$$

其陪集为
$$N, \quad 1+N, \quad 2+N, \quad 3+N$$

在商群中，
$$ (1+N) + (1+N) = 2+N, \qquad (1+N) + (1+N) + (1+N) = 3+N $$

因此 $G/N$ 是一个 4 阶循环群，故
$$\mathbb{Z}_{12}/N \cong \mathbb{Z}_4$$

**直观理解**：把 12 个整数模掉每 4 个一组的等价关系后，只剩下 4 个不同的“位置”。商群就是把“只关心余数”的思想抽象成群结构。

---

## 五、同态基本定理

### 5.1 定理陈述

**定理（同态基本定理）**：设 $f: G \to H$ 是群同态，则
$$G / \ker f \cong \text{Im}\, f$$

### 5.2 证明

**构造同构**：定义 $\bar{f}: G/\ker f \to \text{Im}\, f$，$\bar{f}(g\ker f) = f(g)$。

**良定义性**：若 $g\ker f = g'\ker f$，则 $g^{-1}g' \in \ker f$，$f(g^{-1}g') = e$，故 $f(g) = f(g')$。

**同态**：$\bar{f}((g_1\ker f)(g_2\ker f)) = \bar{f}(g_1g_2\ker f) = f(g_1g_2) = f(g_1)f(g_2)$

**单射**：若 $\bar{f}(g\ker f) = e$，则 $f(g) = e$，$g \in \ker f$，$g\ker f = \ker f$（商群的单位元）。

**满射**：显然。

### 5.3 应用

**例**：$\det: GL_n(\mathbb{R}) \to \mathbb{R}^*$ 是满同态，$\ker(\det) = SL_n(\mathbb{R})$。

故 $GL_n(\mathbb{R}) / SL_n(\mathbb{R}) \cong \mathbb{R}^*$。

---

## 六、同构定理

### 6.1 第一同构定理

**定理**：设 $H \leq G$，$N \trianglelefteq G$，则
$$H/(H \cap N) \cong HN/N$$

### 6.2 第二同构定理

**定理**：设 $N \trianglelefteq G$，$H \leq G$，则
$$(G/N)/(H/N) \cong G/H$$

（前提是 $N \leq H$）

### 6.3 第三同构定理

**定理**：设 $N \trianglelefteq G$，则 $G/N$ 的子群形如 $H/N$，其中 $N \leq H \leq G$。且 $H/N \trianglelefteq G/N$ 当且仅当 $H \trianglelefteq G$。

---

## 七、对应定理

### 7.1 定理

**定理**：设 $N \trianglelefteq G$，则存在双射
$$\{H : N \leq H \leq G\} \longleftrightarrow \{K : K \leq G/N\}$$
$$H \mapsto H/N$$

且保持包含关系和正规性。

### 7.2 应用

用于分析商群的子群结构。

---

## 重要定理汇总

| 定理 | 内容 |
|------|------|
| Lagrange定理 | $\|H\| \mid \|G\|$ |
| 正规子群判别 | $gHg^{-1} = H$ |
| 同态基本定理 | $G/\ker f \cong \text{Im}\, f$ |
| 第一同构定理 | $H/(H \cap N) \cong HN/N$ |
| 第二同构定理 | $(G/N)/(H/N) \cong G/H$ |

---

## 总结

- 陪集把群分成等大小的块，Lagrange 定理由此而来。
- 正规子群是商群存在的前提，商群把“模掉子结构”形式化。
- 同态基本定理说明：群同态本质上是“先商后嵌入”。

## 易错点

- 把任意子群都误认为能取商群，忽略正规性条件。
- 分不清陪集和子群本身，忽略陪集不是群（一般情形下）。
- 商群运算的良定义性没有用到正规子群条件。
- 把指数 $[G:H]$ 和群的阶混淆。

## 练习（分层）

### A档（基础）

1. 写出 $\mathbb{Z}/3\mathbb{Z}$ 的三个陪集。
2. 证明两个陪集要么相等，要么不相交。
3. 用陪集大小说明 $|gH|=|H|$。
4. 计算 $\mathbb{Z}$ 中 $3\mathbb{Z}$ 的指数。

### B档（进阶）

1. 证明 Lagrange 定理。
2. 判断某个子群是否正规。
3. 证明群同态的核一定是正规子群。
4. 计算 $S_3/A_3$ 的结构。

### C档（挑战）

1. 证明同态基本定理。
2. 证明第一同构定理的一个具体例子。
3. 研究 $GL_n(\mathbb{R})/SL_n(\mathbb{R})$ 的结构。
4. 用对应定理分析某个商群的子群结构。

## 相关链接

- [[01_群的定义与基本性质]] - 群的基础概念
- [[03_群的结构]] - 群的分类与结构
- [[06_环的定义与理想]] - 理想是环的"正规子群"

---

**创建时间**：2026年4月3日
**所属模块**：近世代数 → 群论基础
