---
type: note
subject: advanced_algebra
chapter: 04
created: 2026-04-03
updated: 2026-04-25
status: complete
---

# 张量积与外代数

## 📌 学习目标

- 理解双线性映射、张量积的泛性质与构造思路
- 掌握张量积的基、维数与常见计算方法
- 认识外代数、楔积与行列式之间的联系

## ✅ 先修

- [[03_商空间与对偶空间]]
- [[../Linear/23_Linear_Maps|线性映射]]
- [[../Linear/10_Basis_Dimension|基与维数]]
- [[../00_Symbols_Conventions|符号与约定总表]]

## 难度分层

- **基础**：双线性映射、张量积泛性质、简单张量
- **进阶**：张量积基、维数计算、外代数与楔积
- **拓展**：交错多线性映射、行列式联系、普遍性质

## 自测（3问速测）

1. 张量积的泛性质为什么比“直接定义乘积”更重要？
2. 为什么 $\dim(V \otimes W)=\dim V \cdot \dim W$？
3. 外代数中的楔积为什么会和行列式相关？

## 一、张量积

### 1.1 动机

张量积是构造新的线性空间的基本工具，它允许我们将多个线性空间"组合"成一个更大的空间。

**典型问题**：如何定义两个向量空间 $V$ 和 $W$ 的"乘积"，使得其元素像"多项式"？

### 1.2 双线性映射

**定义**：设 $V, W, U$ 是 $F$ 上的线性空间。映射 $\phi: V \times W \to U$ 称为**双线性的**，若：

1. $\phi(v_1 + v_2, w) = \phi(v_1, w) + \phi(v_2, w)$
2. $\phi(\lambda v, w) = \lambda \phi(v, w)$
3. $\phi(v, w_1 + w_2) = \phi(v, w_1) + \phi(v, w_2)$
4. $\phi(v, \lambda w) = \lambda \phi(v, w)$

### 1.3 张量积的定义（泛性质）

**定义**：线性空间 $V \otimes W$ 连同双线性映射 $\otimes: V \times W \to V \otimes W$，满足：

**泛性质**：对任意线性空间 $U$ 和双线性映射 $f: V \times W \to U$，存在唯一的线性映射 $\tilde{f}: V \otimes W \to U$ 使得 $f = \tilde{f} \circ \otimes$。

```
        V × W
       /     \
     ⊗/       \f
     /         \
V ⊗ W ---f̃---> U
```

### 1.4 构造方法

**具体构造**：
1. 考虑以 $V \times W$ 为基的形式线性组合
2. 由双线性关系生成的子空间
3. 作商空间

**元素**：$V \otimes W$ 的元素形如 $\sum_i v_i \otimes w_i$，其中 $(v + v') \otimes w = v \otimes w + v' \otimes w$ 等。

### 1.5 基与维数

**定理**：若 $\{e_1, \ldots, e_n\}$ 是 $V$ 的基，$\{f_1, \ldots, f_m\}$ 是 $W$ 的基，则 $\{e_i \otimes f_j : 1 \le i \le n, 1 \le j \le m\}$ 是 $V \otimes W$ 的基。

**推论**：$\dim(V \otimes W) = \dim V \cdot \dim W$

---

## 二、张量积的性质

### 2.1 基本性质

1. **结合律**：$(U \otimes V) \otimes W \cong U \otimes (V \otimes W)$
2. **交换律**：$V \otimes W \cong W \otimes V$
3. **分配律**：$U \otimes (V \oplus W) \cong (U \otimes V) \oplus (U \otimes W)$
4. **单位元**：$F \otimes V \cong V$

### 2.2 线性映射的张量积

**定义**：设 $S: V \to V'$，$T: W \to W'$ 是线性映射，定义
$$S \otimes T: V \otimes W \to V' \otimes W'$$
在纯张量上：$(S \otimes T)(v \otimes w) = S(v) \otimes T(w)$

**性质**：
- $(S_1 \circ S_2) \otimes (T_1 \circ T_2) = (S_1 \otimes T_1) \circ (S_2 \otimes T_2)$
- $\text{id}_V \otimes \text{id}_W = \text{id}_{V \otimes W}$

### 2.3 与对偶空间的关系

**定理**：$(V \otimes W)^* \cong V^* \otimes W^*$（有限维时）

更一般地，$\mathcal{L}(V, W) \cong V^* \otimes W$。

---

## 三、张量

### 3.1 定义

**定义**：线性空间 $V$ 上的 **$(p, q)$ 型张量**是 $V$ 上 $p$ 个协变、$q$ 个逆变的多重线性泛函：
$$T: \underbrace{V^* \times \cdots \times V^*}_{p} \times \underbrace{V \times \cdots \times V}_{q} \to F$$

**张量空间**：
$$T^p_q(V) = \underbrace{V \otimes \cdots \otimes V}_{p} \otimes \underbrace{V^* \otimes \cdots \otimes V^*}_{q}$$

### 3.2 张量的例子

| 类型 | 空间 | 描述 |
|------|------|------|
| $(0, 0)$ | $F$ | 标量 |
| $(1, 0)$ | $V$ | 向量 |
| $(0, 1)$ | $V^*$ | 共向量/余向量 |
| $(1, 1)$ | $V \otimes V^*$ | 线性算子 |
| $(0, 2)$ | $V^* \otimes V^*$ | 双线性形式 |
| $(2, 0)$ | $V \otimes V$ | 二阶张量 |

### 3.3 Einstein求和约定

**约定**：重复的上下指标表示求和。

例：$A^i_j v^j = \sum_j A^i_j v^j$

**张量变换**：基变换 $e'_i = T^j_i e_j$ 下
- 上指标（逆变）：$v'^i = (T^{-1})^i_j v^j$
- 下指标（协变）：$\omega'_i = T^j_i \omega_j$

---

## 四、外代数

### 4.1 反对称张量

**定义**：张量 $T \in T^p_0(V)$ 称为**反对称的**，若对任意置换 $\sigma \in S_p$：
$$T(v_{\sigma(1)}, \ldots, v_{\sigma(p)}) = \text{sgn}(\sigma) T(v_1, \ldots, v_p)$$

### 4.2 外积

**定义**：外积 $\wedge: T^p_0(V) \times T^q_0(V) \to T^{p+q}_0(V)$ 定义为
$$\alpha \wedge \beta = \frac{1}{p!q!} \sum_{\sigma \in S_{p+q}} \text{sgn}(\sigma) \sigma(\alpha \otimes \beta)$$

**性质**：
- $\alpha \wedge \beta = (-1)^{pq} \beta \wedge \alpha$（反交换律）
- $v \wedge v = 0$（对1-形式）
- 结合律成立

### 4.3 外代数的定义

**定义**：$V$ 的**外代数**（Grassmann代数）定义为
$$\Lambda(V) = \bigoplus_{p=0}^n \Lambda^p(V)$$

其中 $\Lambda^p(V)$ 是 $p$-形式空间，$\dim \Lambda^p(V) = \binom{n}{p}$。

### 4.4 外代数的维数

**定理**：$\dim \Lambda(V) = 2^n$

**证明**：$\sum_{p=0}^n \binom{n}{p} = (1+1)^n = 2^n$

---

## 五、外代数与行列式

### 5.1 行列式的外代数定义

**定理**：设 $T: V \to V$ 是线性映射，则存在唯一的线性映射 $\Lambda^n(T): \Lambda^n(V) \to \Lambda^n(V)$ 使得
$$T(v_1) \wedge \cdots \wedge T(v_n) = \det(T) \cdot v_1 \wedge \cdots \wedge v_n$$

**意义**：行列式是线性映射对最高阶外形式的"缩放因子"。

### 5.2 几何解释

若 $v_1, \ldots, v_n$ 张成的平行体的体积正比于 $v_1 \wedge \cdots \wedge v_n$，则 $\det(T)$ 是该体积在线性变换 $T$ 下的放大倍数。

**完整例题：二维楔积与行列式**

设 $V = \mathbb{R}^2$，标准基为 $e_1, e_2$。取两个向量
$$u = (1, 2) = e_1 + 2e_2, \qquad v = (3, 4) = 3e_1 + 4e_2$$

则它们的楔积为
$$u \wedge v = (e_1 + 2e_2) \wedge (3e_1 + 4e_2)$$
$$= 4e_1 \wedge e_2 + 6e_2 \wedge e_1$$
$$= 4e_1 \wedge e_2 - 6e_1 \wedge e_2 = -2e_1 \wedge e_2$$

这里的系数 $-2$ 正好等于矩阵
$$\begin{pmatrix}1 & 3 \\ 2 & 4\end{pmatrix}$$
的行列式。

**直观理解**：楔积把两个向量张成的“有向面积”编码成一个数；只要 $u \wedge v = 0$，就说明两向量线性相关，张成的平行四边形面积为 0。

---

## 六、对称代数

### 6.1 对称张量

**定义**：张量 $T \in T^p_0(V)$ 称为**对称的**，若对任意置换 $\sigma$：
$$T(v_{\sigma(1)}, \ldots, v_{\sigma(p)}) = T(v_1, \ldots, v_p)$$

### 6.2 对称积

**定义**：对称积 $\odot: T^p_0(V) \times T^q_0(V) \to T^{p+q}_0(V)$ 定义为对称化：
$$\alpha \odot \beta = \frac{1}{p!q!} \sum_{\sigma \in S_{p+q}} \sigma(\alpha \otimes \beta)$$

**性质**：$\alpha \odot \beta = \beta \odot \alpha$（交换律）

### 6.3 对称代数

**定义**：$V$ 的**对称代数**定义为
$$S(V) = \bigoplus_{p=0}^{\infty} S^p(V)$$

其中 $S^p(V)$ 是 $p$ 次对称张量空间。

**与多项式的关系**：$S(V) \cong F[x_1, \ldots, x_n]$（多项式环）

---

## 七、应用

### 7.1 微分形式

在微分几何中，$\Lambda^p(T^*M)$ 是 $p$-微分形式空间，用于定义积分、Stokes定理等。

### 7.2 物理中的张量

- 应力张量：$(0, 2)$ 型张量
- 度量张量：$(0, 2)$ 型张量
- 电磁场张量：$(0, 2)$ 型反对称张量
- Riemann曲率张量：$(1, 3)$ 型张量

### 7.3 计算机科学

- 张量网络：量子计算的表示
- 张量分解：机器学习中的降维

---

## 重要公式汇总

| 概念 | 公式 |
|------|------|
| 张量积维数 | $\dim(V \otimes W) = \dim V \cdot \dim W$ |
| 外代数维数 | $\dim \Lambda(V) = 2^n$ |
| $p$-形式维数 | $\dim \Lambda^p(V) = \binom{n}{p}$ |
| 外积反交换 | $\alpha \wedge \beta = (-1)^{pq} \beta \wedge \alpha$ |
| 行列式定义 | $T(v_1) \wedge \cdots \wedge T(v_n) = \det(T) \cdot v_1 \wedge \cdots \wedge v_n$ |

---

## 总结

- 张量积用泛性质统一了双线性映射的表达，是构造多线性对象的标准语言。
- 外代数是在张量代数上加上交错关系得到的结构，楔积是其核心运算。
- 行列式可以看作顶层外幂上的交错多线性形式，和外代数天然相连。

## 易错点

- 把张量积当成普通集合乘积，忽略其由泛性质定义。
- 忘记张量积中的关系来自双线性，而不是任意线性关系。
- 计算基时漏掉简单张量之间的线性关系。
- 把楔积的反对称性和普通乘法的交换律混淆。

## 练习（分层）

### A档（基础）

1. 写出 $v \otimes w$ 的定义并说明它为什么是简单张量。
2. 证明张量积对加法和数乘的分配性。
3. 计算 $F^m \otimes F^n$ 的维数。
4. 写出两个向量的楔积定义。

### B档（进阶）

1. 证明 $\dim(V \otimes W)=\dim V \cdot \dim W$。
2. 说明一个双线性映射如何通过张量积唯一分解。
3. 计算一个具体张量在不同基下的坐标。
4. 说明外代数中 $v \wedge v = 0$ 的原因。

### C档（挑战）

1. 证明张量积的泛性质。
2. 证明外代数的普遍性质。
3. 将一个交错多线性映射因子分解为楔积诱导映射。
4. 说明行列式为何可由顶层外幂诱导得到。

## 相关链接

- [[03_商空间与对偶空间]] - 对偶空间基础
- [[01_Determinants|行列式]] - 行列式的传统定义
- [[../Abstract/10_模的基础理论|模论]] - 张量积的模论推广

---

**创建时间**：2026年4月3日
**所属模块**：高等代数 → 线性空间深化
