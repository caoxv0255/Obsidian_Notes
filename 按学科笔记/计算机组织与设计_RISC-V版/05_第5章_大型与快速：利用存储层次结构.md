# 第5章 大型与快速：利用存储层次结构

> **本章目标**：深入理解存储层次结构，掌握缓存和虚拟内存的工作原理

---

## 📖 章节概述

本章深入探讨存储系统的设计，包括：

- 存储技术
- 缓存原理
- 虚拟内存
- 存储层次结构
- 缓存性能优化
- 存储一致性

通过本章学习，你将理解如何设计高效的存储系统，平衡速度、容量和成本。

---

## 🎯 学习目标

完成本章学习后，您应该能够：

1. ✅ 理解存储层次结构的设计原理
2. ✅ 掌握缓存的基本工作原理
3. ✅ 理解虚拟内存的管理机制
4. ✅ 掌握缓存性能评估方法
5. ✅ 理解TLB的作用
6. ✅ 了解存储一致性协议
7. ✅ 能够优化程序以利用存储层次结构

---

## 📚 核心内容

### 5.1 引言

#### 存储层次结构的需求

**理想存储器**：
- 速度快（接近寄存器）
- 容量大（接近磁盘）
- 成本低（接近磁盘）

**现实情况**：
- 速度、容量、成本三者不可兼得
- 需要通过层次结构平衡

#### 存储层次结构

```
        ┌──────────────┐
        │    寄存器    │  ← 最快、最小、最贵
        └──────────────┘
               ↓
        ┌──────────────┐
        │   L1缓存     │
        └──────────────┘
               ↓
        ┌──────────────┐
        │   L2缓存     │
        └──────────────┘
               ↓
        ┌──────────────┐
        │   L3缓存     │
        └──────────────┘
               ↓
        ┌──────────────┐
        │   主存(DRAM) │
        └──────────────┘
               ↓
        ┌──────────────┐
        │   磁盘/SSD   │
        └──────────────┘
               ↓
        ┌──────────────┐
        │  网络存储    │  ← 最慢、最大、最便宜
        └──────────────┘
```

#### 局部性原理

**时间局部性(Temporal Locality)**：
- 如果一个数据被访问，它很可能在不久的将来再次被访问
- 示例：循环变量、栈变量

**空间局部性(Spatial Locality)**：
- 如果一个数据被访问，它附近的数据很可能在不久的将来被访问
- 示例：数组元素、顺序代码

---

### 5.2 存储技术

#### SRAM（静态随机存取存储器）

**特点**：
- 静态：不需要刷新
- 速度快：访问时间约1-10ns
- 密度低：每比特成本高
- 功耗：工作时功耗高，待机功耗低

**应用**：缓存（L1、L2、L3）

**结构**：
- 6个晶体管存储1位
- 触发器结构

#### DRAM（动态随机存取存储器）

**特点**：
- 动态：需要定期刷新
- 速度中等：访问时间约50-100ns
- 密度高：每比特成本低
- 功耗：工作时功耗中等

**应用**：主存储器

**结构**：
- 1个晶体管+1个电容存储1位
- 需要刷新

**DRAM类型**：
- **SDRAM**：同步DRAM
- **DDR**：双倍数据速率SDRAM
- **DDR2/DDR3/DDR4/DDR5**：改进版本

#### Flash Memory

**特点**：
- 非易失：断电后数据不丢失
- 可擦除：可以多次擦写
- 密度高：每比特成本中等
- 速度慢：擦写速度慢于DRAM

**应用**：SSD、USB闪存盘、手机存储

**类型**：
- **NOR Flash**：随机访问快，密度低
- **NAND Flash**：密度高，块访问

#### 存储技术对比

| 技术 | 速度 | 密度 | 成本 | 易失性 | 应用 |
|------|------|------|------|--------|------|
| SRAM | 快 | 低 | 高 | 是 | 缓存 |
| DRAM | 中 | 高 | 中 | 是 | 主存 |
| Flash | 慢 | 高 | 中 | 否 | SSD |
| 磁盘 | 很慢 | 很高 | 低 | 否 | 存储 |

---

### 5.3 缓存基础

#### 缓存的基本概念

**定义**：小容量、高速存储器，存储常用数据

**目的**：减少访问慢速存储器的次数

**工作原理**：
1. CPU请求数据
2. 检查缓存
3. 命中：直接从缓存读取
4. 不命中：从主存读取并放入缓存

#### 缓存的组织

**块/行(Block/Line)**：
- 缓存和主存之间传输的基本单位
- 大小：通常32-128字节

**组(Set)**：
- 多个块的集合
- 直接映射：1个块/组
- 组相联：多个块/组

**路(Way)**：
- 组中的块数
- 直接映射：1路
- N路组相联：N路

#### 缓存映射方式

**1. 直接映射(Direct Mapped)**：
```
主存地址 = 标记 | 组索引 | 块偏移
```

**优点**：
- 简单
- 访问快

**缺点**：
- 冲突率高

**2. 全相联(Fully Associative)**：
- 块可以放在任何位置

**优点**：
- 冲突率低

**缺点**：
- 复杂
- 访问慢

**3. 组相联(Set Associative)**：
- 块可以放在特定组的任意位置

**优点**：
- 平衡复杂度和性能

**缺点**：
- 中等复杂度

#### 缓存读操作

**流程**：
1. CPU发送地址
2. 提取组索引和标记
3. 检查该组的所有路
4. 如果标记匹配且有效：命中
5. 如果不匹配：不命中

#### 缓存写操作

**写策略**：

**1. 写通(Write Through)**：
- 数据同时写入缓存和主存
- 简单但慢

**2. 写回(Write Back)**：
- 数据只写入缓存
- 脏块：标记为已修改
- 替换时写回主存
- 快但复杂

**写缺失策略**：

**1. 写分配(Write Allocate)**：
- 将块加载到缓存
- 然后写入

**2. 不写分配(No Write Allocate)**：
- 直接写入主存
- 不加载到缓存

#### 缓存替换策略

**1. 随机替换(Random)**：
- 随机选择一个块替换

**2. 最近最少使用(LRU, Least Recently Used)**：
- 替换最久未使用的块
- 需要记录访问历史

**3. 先进先出(FIFO, First In First Out)**：
- 替换最早加载的块

#### 示例

**直接映射缓存**：
- 缓存大小：8KB
- 块大小：32字节
- 组数：256（8KB / 32字节）
- 标记位数：32 - log₂(256) - log₂(32) = 24位

**访问地址0x12345678**：
- 块偏移：0x18（5位）
- 组索引：0xA（8位）
- 标记：0x123456（19位）

---

### 5.4 测量和改进缓存性能

#### 性能指标

**命中率(Hit Rate)**：
```
命中率 = 命中次数 / 总访问次数
```

**不命中率(Miss Rate)**：
```
不命中率 = 不命中次数 / 总访问次数
```

**平均访问时间(AMAT, Average Memory Access Time)**：
```
AMAT = 命中时间 + 不命中率 × 不命中代价
```

**示例**：
- 命中时间：1周期
- 不命中率：5%
- 不命中代价：100周期

```
AMAT = 1 + 0.05 × 100 = 6周期
```

#### 不命中类型

**1. 强制不命中(Compulsory Miss) / 冷不命中**：
- 首次访问数据时必然不命中
- 预加载可以减少

**2. 容量不命中(Capacity Miss)**：
- 缓存容量不够，块被替换
- 增加缓存大小可以减少

**3. 冲突不命中(Conflict Miss)**：
- 多个块映射到同一位置
- 增加相联度可以减少

#### 优化技术

**1. 更大的缓存**：
- 减少容量不命中
- 增加成本和功耗

**2. 更大的块大小**：
- 利用空间局部性
- 减少不命中率但增加不命中代价

**3. 更高的相联度**：
- 减少冲突不命中
- 增加复杂度和延迟

**4. 多级缓存**：
- L1：小而快
- L2：较大
- L3：最大
- 平衡性能和成本

**5. 优先读取数据(Critical Word First)**：
- 不命中时优先返回请求的数据
- 立即启动写回
- 减少停顿时间

**6. 合并写缓冲(Merging Write Buffer)**：
- 合并对同一块的多次写
- 减少总线流量

**7. 编译器优化**：
- 循环优化
- 数组填充
- 数据预取

---

### 5.5 可靠的存储层次结构

#### 错误检测和纠正

**奇偶校验(Parity)**：
- 添加1位校验位
- 检测单个位错误
- 不能纠正错误

**汉明码(Hamming Code)**：
- 使用多个校验位
- 可以检测和纠正单个位错误
- 可以检测两个位错误

**ECC（Error Correcting Code）**：
- 可以纠正多位错误
- 常用于服务器内存

#### 容错设计

**RAID（冗余磁盘阵列）**：
- **RAID 0**：条带化，无冗余
- **RAID 1**：镜像
- **RAID 5**：分布式奇偶校验
- **RAID 6**：双奇偶校验
- **RAID 10**：镜像+条带化

**闪存纠错**：
- BCH码
- LDPC码

---

### 5.6 虚拟机

#### 虚拟机的概念

**定义**：在物理机上模拟多个虚拟机

**优势**：
- 资源隔离
- 灵活管理
- 提高利用率

**类型**：
- **类型1（裸机）**：直接运行在硬件上
- **类型2（托管）**：运行在操作系统上

#### 虚拟化技术

**CPU虚拟化**：
- 指令模拟
- 陷阱和模拟

**内存虚拟化**：
- 影子页表
- 硬件辅助虚拟化（EPT/NPT）

**I/O虚拟化**：
- 模拟设备
- 直通设备
- SR-IOV

---

### 5.7 虚拟内存

#### 虚拟内存的概念

**定义**：将虚拟地址映射到物理地址

**目的**：
- 提供大于物理内存的地址空间
- 实现内存保护
- 简化内存管理

#### 地址翻译

**虚拟地址空间**：
- 每个进程有独立的虚拟地址空间
- 32位：4GB
- 64位：非常大

**物理地址空间**：
- 实际物理内存
- 通常小于虚拟地址空间

**页表(Page Table)**：
- 存储虚拟地址到物理地址的映射
- 每个进程有自己的页表

#### 分页

**页(Page)**：
- 虚拟内存和物理内存的固定大小块
- 通常4KB

**页表项(PTE, Page Table Entry)**：
- 物理页号
- 有效位
- 读写位
- 用户/内核位
- 脏位
- 访问位

**多级页表**：
- 减少页表大小
- 层次结构

#### TLB（Translation Lookaside Buffer）

**定义**：缓存常用的页表项

**目的**：加速地址翻译

**工作原理**：
1. CPU发送虚拟地址
2. 检查TLB
3. 命中：直接获得物理地址
4. 不命中：查页表，更新TLB

**TLB不命中**：
- 软件处理：操作系统处理
- 硬件处理：硬件自动处理

#### 页面错误(Page Fault)

**定义**：访问的页面不在物理内存中

**处理流程**：
1. 检测页面错误
2. 操作系统接管
3. 加载页面到物理内存
4. 更新页表
5. 重启指令

#### 示例

**虚拟地址翻译**：
- 虚拟地址：0x12345678
- 页大小：4KB
- 虚拟页号：0x12345
- 页偏移：0x678
- 页表查找：虚拟页号 → 物理页号
- 物理地址：物理页号 × 4KB + 0x678

---

### 5.8 存储层次结构的通用框架

#### 多级缓存

**包含性(Inclusion)**：
- 上级缓存的数据是否一定在下级缓存中
- 严格包含：是
- 非包含：否

**一致性(Coherence)**：
- 确保各级缓存的数据一致

#### 替换策略

**全局替换**：
- 在所有缓存级别统一管理替换

**局部替换**：
- 每个缓存级别独立管理替换

---

### 5.9 使用有限状态机控制简单缓存

#### 有限状态机(FSM)

**定义**：具有有限状态的系统

**状态**：
- 空闲
- 检查缓存
- 加载数据
- 存储数据

**转换**：
- 基于输入和当前状态
- 转换到下一个状态

#### 缓存控制器

**读操作FSM**：
```
空闲 → 检查缓存 → 命中/不命中 → 返回数据 → 空闲
```

**写操作FSM**：
```
空闲 → 检查缓存 → 命中/不命中 → 写入数据 → 空闲
```

---

### 5.10 并行性和存储层次结构：缓存一致性

#### 多核处理器的问题

**问题**：多个核心共享缓存或主存

**挑战**：
- 数据一致性
- 缓存一致性协议

#### 缓存一致性协议

**写无效协议(Write Invalidate)**：
- 写入时使其他核心的缓存副本无效
- 适合写入多的场景

**写更新协议(Write Update)**：
- 写入时更新其他核心的缓存副本
- 适合读取多的场景

**MESI协议**：
- **M(Modified)**：已修改，只在一个缓存中
- **E(Exclusive)**：独占，只在一个缓存中，与内存一致
- **S(Shared)**：共享，在多个缓存中
- **I(Invalid)**：无效

#### 协议示例

**写入操作**：
1. 核心A写入数据
2. 核心A使其他核心的缓存副本无效
3. 核心A的缓存状态变为M

**读取操作**：
1. 核心B读取数据
2. 如果数据在其他核心的M状态，写回内存
3. 核心B加载缓存副本
4. 核心B的缓存状态变为S

---

### 5.11 并行性和存储层次结构：廉价磁盘冗余阵列

#### RAID概念

**RAID**：将多个磁盘组合成一个逻辑磁盘

**目标**：
- 提高性能
- 提高可靠性
- 降低成本

#### RAID级别

**RAID 0（条带化）**：
- 数据分布在多个磁盘上
- 提高性能
- 无冗余

**RAID 1（镜像）**：
- 数据复制到多个磁盘
- 提高可靠性
- 成本高

**RAID 5（分布式奇偶校验）**：
- 数据和奇偶校验分布在多个磁盘上
- 提高性能和可靠性
- 允许一个磁盘故障

**RAID 6（双奇偶校验）**：
- 两个奇偶校验
- 允许两个磁盘故障

**RAID 10（镜像+条带化）**：
- 结合RAID 1和RAID 0
- 高性能和高可靠性

---

### 5.12 高级主题：实现缓存控制器

#### 缓存控制器的复杂性

**功能**：
- 地址解码
- 标记比较
- 数据选择
- 替换逻辑
- 一致性协议

**实现**：
- 硬件控制器
- 微代码
- 组合方法

---

### 5.13 实际案例：ARM Cortex-A8和Intel Core i7存储层次结构

#### ARM Cortex-A8

**L1缓存**：
- 指令缓存：32KB，4路组相联
- 数据缓存：32KB，4路组相联

**L2缓存**：
- 大小：256KB-1MB
- 相联度：8路组相联

**TLB**：
- 指令TLB：32项
- 数据TLB：32项

#### Intel Core i7

**L1缓存**：
- 指令缓存：32KB，8路组相联
- 数据缓存：32KB，8路组相联

**L2缓存**：
- 大小：256KB
- 相联度：8路组相联

**L3缓存**：
- 大小：8MB
- 相联度：16路组相联

**TLB**：
- 指令TLB：128项
- 数据TLB：64项

#### 对比

| 特性 | ARM Cortex-A8 | Intel Core i7 |
|------|---------------|---------------|
| L1指令缓存 | 32KB | 32KB |
| L1数据缓存 | 32KB | 32KB |
| L2缓存 | 256KB-1MB | 256KB |
| L3缓存 | 无 | 8MB |
| TLB | 32项 | 128项（指令）/64项（数据） |

---

### 5.14 实际案例：RISC-V系统的其余部分和特殊指令

#### RISC-V存储系统

**缓存**：
- 可选L1缓存
- 可选L2缓存

**TLB**：
- 可选TLB
- 可选硬件页表遍历

**特殊指令**：
- **fence**：内存屏障
- **fence.i**：指令同步屏障
- **sfence.vma**：TLB刷新

---

### 5.15 加速运行：缓存分块和矩阵乘法

#### 缓存分块

**问题**：大矩阵乘法导致缓存抖动

**解决方案**：将矩阵分成小块，适合缓存

**示例**：
```c
#define BLOCK_SIZE 32

void matrix_multiply_blocked(float *A, float *B, float *C, int n) {
    for (int i = 0; i < n; i += BLOCK_SIZE) {
        for (int j = 0; j < n; j += BLOCK_SIZE) {
            for (int k = 0; k < n; k += BLOCK_SIZE) {
                for (int ii = i; ii < i + BLOCK_SIZE; ii++) {
                    for (int jj = j; jj < j + BLOCK_SIZE; jj++) {
                        float sum = 0;
                        for (int kk = k; kk < k + BLOCK_SIZE; kk++) {
                            sum += A[ii*n + kk] * B[kk*n + jj];
                        }
                        C[ii*n + jj] += sum;
                    }
                }
            }
        }
    }
}
```

#### 性能提升

**优化技术**：
1. 缓存分块：提高缓存利用率
2. 循环展开：减少循环开销
3. 数据预取：提前加载数据
4. 寄存器分配：减少内存访问

**性能提升**：
- 标准：1x
- 缓存分块：2-4x
- 综合优化：5-10x

---

### 5.16 误区与陷阱

#### 常见误区

1. **误区**：缓存越大越好
   **事实**：缓存越大，访问时间越长，成本越高

2. **误区**：命中率越高越好
   **事实**：提高命中率可能增加访问时间

3. **误区**：虚拟内存没有开销
   **事实**：地址翻译有开销

#### 常见陷阱

1. **陷阱**：忽略缓存一致性
   **后果**：数据不一致

2. **陷阱**：忽略TLB不命中
   **后果**：性能下降

3. **陷阱**：忽略页面错误
   **后果**：性能下降

#### 最佳实践

1. **利用局部性**：
   - 访问连续内存
   - 重用数据

2. **优化数据结构**：
   - 选择合适的数据结构
   - 考虑缓存行大小

3. **避免缓存抖动**：
   - 使用缓存分块
   - 优化循环

---

### 5.17 总结

#### 本章要点

1. **存储层次结构**
   - 平衡速度、容量、成本
   - 利用局部性原理

2. **缓存**
   - 直接映射、全相联、组相联
   - 命中率、不命中率、AMAT
   - 写策略、替换策略

3. **虚拟内存**
   - 地址翻译
   - 页表
   - TLB
   - 页面错误

4. **性能优化**
   - 增加缓存大小
   - 提高相联度
   - 缓存分块
   - 数据预取

5. **多核一致性**
   - 缓存一致性协议
   - MESI协议

6. **RAID**
   - 提高性能和可靠性
   - 不同RAID级别

#### 学习建议

1. **理解原理**：深入理解存储层次结构的工作原理
2. **动手实践**：测量程序的缓存性能
3. **性能优化**：优化程序以利用存储层次结构
4. **持续学习**：关注最新的存储技术

---

## 🔑 关键术语

| 术语 | 英文 | 定义 |
|------|------|------|
| **SRAM** | Static Random Access Memory | 静态随机存取存储器 |
| **DRAM** | Dynamic Random Access Memory | 动态随机存取存储器 |
| **Cache** | - | 缓存 |
| **Block** | - | 块 |
| **Set** | - | 组 |
| **Way** | - | 路 |
| **Hit Rate** | - | 命中率 |
| **Miss Rate** | - | 不命中率 |
| **AMAT** | Average Memory Access Time | 平均访问时间 |
| **TLB** | Translation Lookaside Buffer | 地址翻译后备缓冲器 |
| **Page Table** | - | 页表 |
| **Page Fault** | - | 页面错误 |
| **RAID** | Redundant Array of Inexpensive Disks | 廉价磁盘冗余阵列 |
| **MESI** | Modified/Exclusive/Shared/Invalid | 缓存一致性协议 |
| **Locality** | - | 局部性 |

---

## 📝 自我检查

### 检查问题

1. 什么是存储层次结构？为什么要使用存储层次结构？
2. 解释时间局部性和空间局部性。
3. SRAM和DRAM有什么区别？
4. 什么是缓存？缓存的基本工作原理是什么？
5. 比较直接映射、全相联和组相联缓存的优缺点。
6. 什么是AMAT？如何计算AMAT？
7. 列举三种缓存不命中类型。
8. 什么是虚拟内存？虚拟内存有什么优势？
9. 什么是TLB？TLB的作用是什么？
10. 什么是缓存一致性？为什么需要缓存一致性？

### 答案

<details>
<summary>点击查看答案</summary>

1. **存储层次结构**：多级存储系统，平衡速度、容量、成本
   **原因**：理想存储器不存在，需要层次结构平衡

2. **时间局部性**：如果数据被访问，很可能再次被访问
   **空间局部性**：如果数据被访问，附近的数据很可能被访问

3. **区别**：
   - **SRAM**：静态、快、贵、密度低
   - **DRAM**：动态、慢、便宜、密度高

4. **缓存**：小容量、高速存储器，存储常用数据
   **工作原理**：检查缓存，命中则读取，不命中则从主存加载

5. **比较**：
   - **直接映射**：简单、快、冲突率高
   - **全相联**：复杂、慢、冲突率低
   - **组相联**：平衡

6. **AMAT**：平均访问时间
   **计算**：AMAT = 命中时间 + 不命中率 × 不命中代价

7. **不命中类型**：
   - 强制不命中（冷不命中）
   - 容量不命中
   - 冲突不命中

8. **虚拟内存**：虚拟地址映射到物理地址
   **优势**：
   - 大地址空间
   - 内存保护
   - 简化管理

9. **TLB**：缓存常用的页表项
   **作用**：加速地址翻译

10. **缓存一致性**：确保多个核心的缓存数据一致
    **原因**：多核处理器共享缓存或主存

</details>

---

## 🎯 课后练习

### 基础练习

1. 计算以下缓存的参数：
   - 缓存大小：16KB
   - 块大小：64字节
   - 直接映射

2. 计算AMAT：
   - 命中时间：2周期
   - 不命中率：3%
   - 不命中代价：50周期

3. 解释页面错误的处理流程。

### 进阶练习

4. 设计一个4路组相联缓存控制器。

5. 分析MESI协议的状态转换。

6. 实现一个简单的虚拟内存管理器。

### 挑战练习

7. 优化一个矩阵乘法程序以利用缓存。

8. 分析不同缓存配置对性能的影响。

9. 实现一个缓存一致性协议。

---

**本章结束** → 下一章：[[06_第6章_从客户端到云端的并行处理器]]