# 附录B 图形与计算GPU

> **附录目标**：深入了解GPU架构和编程，掌握并行计算的基本原理

---

## 📖 附录概述

本附录介绍图形处理器(GPU)的架构和编程，包括：

- GPU系统架构
- GPU编程
- 多线程多处理器架构
- 并行内存系统
- 浮点运算

通过本附录学习，你将理解GPU如何高效处理并行计算任务。

---

## 📚 核心内容

### B.1 引言

#### GPU的历史

**起源**：用于图形渲染
**发展**：通用GPU计算(GPGPU)
**现状**：并行计算的重要平台

#### GPU vs CPU

| 特性 | CPU | GPU |
|------|-----|-----|
| 核心数 | 少（4-64） | 多（数千） |
| 核心复杂度 | 高 | 低 |
| 内存带宽 | 中等 | 高 |
| 适用场景 | 串行、复杂任务 | 并行、简单任务 |

---

### B.2 GPU系统架构

#### GPU架构组成

**1. 多处理器(Multiprocessor)**：
- 包含多个CUDA核心
- 共享内存
- 寄存器

**2. 内存层次**：
- 全局内存
- 共享内存
- 常量内存
- 纹理内存

**3. 线程层次**：
- 线程(Thread)
- 线程块(Block)
- 线程网格(Grid)

#### SIMT架构

**定义**：单指令多线程

**特点**：
- 32个线程一组（warp）
- 同一条指令处理多个线程
- 掩码执行处理分支

---

### B.3 GPU编程

#### CUDA编程模型

**层次结构**：
```
Grid (网格)
  └─ Block (块)
      └─ Thread (线程)
```

**CUDA C示例**：
```c
__global__ void add(float *a, float *b, float *c, int n) {
    int i = blockIdx.x * blockDim.x + threadIdx.x;
    if (i < n) {
        c[i] = a[i] + b[i];
    }
}

int main() {
    float *d_a, *d_b, *d_c;
    int n = 1024;

    // 分配内存
    cudaMalloc(&d_a, n * sizeof(float));
    cudaMalloc(&d_b, n * sizeof(float));
    cudaMalloc(&d_c, n * sizeof(float));

    // 拷贝数据到GPU
    cudaMemcpy(d_a, a, n * sizeof(float), cudaMemcpyHostToDevice);
    cudaMemcpy(d_b, b, n * sizeof(float), cudaMemcpyHostToDevice);

    // 启动核函数
    add<<<(n+255)/256, 256>>>(d_a, d_b, d_c, n);

    // 拷贝结果回CPU
    cudaMemcpy(c, d_c, n * sizeof(float), cudaMemcpyDeviceToHost);

    // 释放内存
    cudaFree(d_a);
    cudaFree(d_b);
    cudaFree(d_c);

    return 0;
}
```

#### 内存模型

**全局内存**：
- 大容量
- 高延迟
- 所有线程可访问

**共享内存**：
- 小容量
- 低延迟
- 块内线程共享

**常量内存**：
- 只读
- 缓存优化

**纹理内存**：
- 只读
- 空间局部性优化

---

### B.4 多线程多处理器架构

#### 多处理器(Multiprocessor)

**组成**：
- CUDA核心
- 共享内存
- 寄存器
- 特殊功能单元

**调度**：
- warp调度
- 延迟隐藏
- 线程切换

#### 性能优化

**1. 合并访问**：
- 连续访问全局内存
- 减少内存事务

**2. 避免分支分歧**：
- 减少warp内分支
- 使用掩码

**3. 共享内存使用**：
- 利用共享内存
- 减少全局内存访问

---

### B.5 并行内存系统

#### 内存合并

**定义**：多个线程的内存访问合并为一个事务

**优势**：
- 提高带宽利用率
- 减少延迟

**示例**：
```
线程0：访问地址0
线程1：访问地址4
线程2：访问地址8
线程3：访问地址12
→ 合并为一个事务
```

#### 内存银行冲突

**定义**：多个线程同时访问同一内存银行

**后果**：
- 降低性能
- 串行化访问

**解决**：
- 重新组织数据访问
- 使用填充

---

### B.6 浮点运算

#### GPU浮点性能

**特点**：
- 高吞吐量
- 支持多种精度

**精度类型**：
- FP32（单精度）
- FP64（双精度）
- FP16（半精度）
- INT8（8位整数）

#### Tensor Core

**定义**：专用矩阵乘法加速器

**性能**：
- 极高的矩阵乘法性能
- 支持混合精度

---

### B.7 实际案例：NVIDIA GeForce 8800

#### 架构特点

**核心数**：128个CUDA核心
**内存**：768MB GDDR3
**带宽**：86.4 GB/s

#### 创新

**统一着色器架构**：
- 不区分顶点和像素着色器
- 灵活分配资源

---

### B.8 实际案例：将应用映射到GPU

#### 应用优化步骤

1. **识别可并行部分**
2. **数据分解**
3. **内存优化**
4. **性能测量**
5. **迭代优化**

#### 示例：矩阵乘法

**CPU版本**：
```c
for (int i = 0; i < n; i++) {
    for (int j = 0; j < n; j++) {
        for (int k = 0; k < n; k++) {
            C[i][j] += A[i][k] * B[k][j];
        }
    }
}
```

**GPU版本**：
```c
__global__ void matrix_mul(float *A, float *B, float *C, int n) {
    int row = blockIdx.y * blockDim.y + threadIdx.y;
    int col = blockIdx.x * blockDim.x + threadIdx.x;
    float sum = 0;

    for (int k = 0; k < n; k++) {
        sum += A[row * n + k] * B[k * n + col];
    }

    C[row * n + col] = sum;
}
```

---

### B.9 误区与陷阱

#### 常见误区

1. **误区**：GPU总是比CPU快
   **事实**：取决于应用和数据规模

2. **误区**：更多线程总是更好
   **事实**：受限于资源

#### 常见陷阱

1. **陷阱**：忽略内存访问模式
   **后果**：性能下降

2. **陷阱**：忽略分支分歧
   **后果**：warp串行化

---

### B.10 总结

#### 附录要点

1. **GPU架构**：多处理器、SIMT、内存层次
2. **GPU编程**：CUDA、线程层次、内存模型
3. **性能优化**：合并访问、共享内存、避免分歧
4. **应用映射**：识别并行、数据分解、优化

#### 学习建议

1. **理解架构**：深入理解GPU架构
2. **动手实践**：编写CUDA程序
3. **性能分析**：使用profiler分析性能
4. **持续学习**：关注最新的GPU技术

---

**附录B结束** → 附录C：[[附录C_控制映射到硬件]]