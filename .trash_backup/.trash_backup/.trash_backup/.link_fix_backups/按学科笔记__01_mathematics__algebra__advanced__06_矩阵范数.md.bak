---
type: note
subject: advanced_algebra
chapter: 06
created: 2026-04-03
updated: 2026-04-25
status: complete
---

# 矩阵范数

## 📌 学习目标

- 理解向量范数、矩阵范数与诱导范数的关系
- 掌握矩阵范数的基本性质、等价性和常见不等式
- 能用谱范数、Frobenius 范数和条件数分析矩阵行为

## ✅ 先修

- [[05_线性变换的深入理论]]
- [[21_SVD|奇异值分解]]
- [[../Linear/23_Linear_Maps|线性映射]]
- [[../00_Symbols_Conventions|符号与约定总表]]

## 难度分层

- **基础**：向量范数、矩阵范数、诱导范数
- **进阶**：谱范数、Frobenius 范数、范数等价性
- **拓展**：条件数、谱半径界、数值稳定性

## 自测（3问速测）

1. 诱导矩阵范数为什么一定满足次乘性？
2. 谱范数和奇异值之间有什么关系？
3. 条件数为什么能描述线性方程组的敏感性？

## 一、向量范数回顾

### 1.1 定义

**定义**：向量空间 $V$ 上的**范数**是函数 $\|\cdot\|: V \to \mathbb{R}$，满足：

$$\kappa_2(A) = \|A\|_2 \|A^{-1}\|_2 = \frac{\sigma_{\max}(A)}{\sigma_{\min}(A)}$$
### 2.1 定义

**定义**：矩阵空间 $\mathbb{C}^{m \times n}$ 上的**矩阵范数**是满足向量范数三条性质的函数 $\|\cdot\|: \mathbb{C}^{m \times n} \to \mathbb{R}$。

### 2.2 相容性（次乘性）

**定义**：矩阵范数称为**相容的**（或满足次乘性），若对可相乘的矩阵 $A, B$：
$$\|AB\| \leq \|A\| \|B\|$$

### 2.3 与向量范数的相容性

**定义**：矩阵范数 $\|\cdot\|_M$ 与向量范数 $\|\cdot\|_v$ **相容**，若
$$\|Ax\|_v \leq \|A\|_M \|x\|_v$$

---


### 7.5 完整例题：对角矩阵的范数与条件数

设
$$A = \begin{pmatrix}4 & 0 \\ 0 & \frac{1}{2}\end{pmatrix}$$

则 1-范数、无穷范数和 Frobenius 范数分别为
$$\|A\|_1 = \max\{4, 1/2\} = 4$$
$$\|A\|_\infty = \max\{4, 1/2\} = 4$$
$$\|A\|_F = \sqrt{4^2 + (1/2)^2} = \frac{\sqrt{65}}{2}$$

因为 $A$ 是对角矩阵，2-范数就是对角元绝对值的最大值：
$$\|A\|_2 = 4$$

其逆矩阵为
$$A^{-1} = \begin{pmatrix}\frac{1}{4} & 0 \\ 0 & 2\end{pmatrix}$$

因此
$$\|A^{-1}\|_2 = 2, \qquad \kappa_2(A) = 4 \times 2 = 8$$

**直观理解**：这个矩阵把一个方向放大 4 倍，另一个方向缩小到原来的 1/2 倍，所以它对不同方向的“拉伸不均匀”。条件数 8 正是在量化这种不均匀性和对扰动的敏感程度。
## 三、算子范数（诱导范数）

### 3.1 定义

**定义**：由向量范数 $\|\cdot\|_v$ **诱导**的矩阵范数定义为
$$\|A\| = \sup_{x \neq 0} \frac{\|Ax\|_v}{\|x\|_v} = \max_{\|x\|_v = 1} \|Ax\|_v$$

算子范数自动满足相容性。

### 3.2 常见算子范数

**定理**：

| 诱导向量范数 | 矩阵算子范数 |
|--------------|--------------|
| $\|\cdot\|_1$ | $\|A\|_1 = \max_j \sum_i |a_{ij}|$（列和范数） |
| $\|\cdot\|_\infty$ | $\|A\|_\infty = \max_i \sum_j |a_{ij}|$（行和范数） |
| $\|\cdot\|_2$ | $\|A\|_2 = \sigma_{\max}(A)$（谱范数） |

### 3.3 谱范数的性质

**定理**：设 $A \in \mathbb{C}^{m \times n}$，则
$$\|A\|_2 = \sqrt{\lambda_{\max}(A^* A)} = \sigma_{\max}(A)$$

其中 $\sigma_{\max}(A)$ 是 $A$ 的最大奇异值。

**性质**：
- $\|A\|_2 = \|A^*\|_2 = \|A^T\|_2$
- $\|A\|_2^2 = \|A^*A\|_2 = \|AA^*\|_2$
- $\|A\|_2 = \max_{\|x\|_2=\|y\|_2=1} |y^* A x|$

---

## 四、Frobenius范数

### 4.1 定义

**定义**：矩阵 $A \in \mathbb{C}^{m \times n}$ 的 **Frobenius范数**（或Hilbert-Schmidt范数）定义为
$$\|A\|_F = \sqrt{\sum_{i,j} |a_{ij}|^2} = \sqrt{\text{tr}(A^*A)}$$

### 4.2 性质

1. $\|A\|_F = \sqrt{\sum_i \sigma_i^2}$（奇异值的平方和）
2. $\|A\|_F = \|A^*\|_F$
3. $\|AB\|_F \leq \|A\|_F \|B\|_F$（次乘性成立）
4. $\|A\|_2 \leq \|A\|_F \leq \sqrt{r} \|A\|_2$（$r = \text{rank}(A)$）
5. 与内积的关系：$\langle A, B \rangle_F = \text{tr}(A^*B)$

### 4.3 Frobenius范数 vs 谱范数

| 性质 | 谱范数 $\|\cdot\|_2$ | Frobenius范数 $\|\cdot\|_F$ |
|------|----------------------|------------------------------|
| 诱导方式 | 由向量2-范数诱导 | 非诱导范数 |
| 计算复杂度 | 需要求最大奇异值 | 直接求和 |
| 不变性 | 酉不变 | 酉不变 |
| 与秩的关系 | 无直接关系 | $\|A\|_F^2 = \sum \sigma_i^2$ |

---

## 五、范数的性质与不等式

### 5.1 基本不等式

**矩阵范数与谱半径**：
$$\rho(A) \leq \|A\|$$
对任意矩阵范数成立。

**谱范数与Frobenius范数**：
$$\|A\|_2 \leq \|A\|_F \leq \sqrt{n}\|A\|_2$$

### 5.2 矩阵幂次的范数

**定理**：若 $\|A\| < 1$，则 $I - A$ 可逆，且
$$(I - A)^{-1} = \sum_{k=0}^\infty A^k$$
$$\|(I - A)^{-1}\| \leq \frac{1}{1 - \|A\|}$$

### 5.3 谱半径的极限表示

**定理**：
$$\rho(A) = \lim_{k \to \infty} \|A^k\|^{1/k}$$
对任意矩阵范数成立。

---

## 六、矩阵序列与收敛

### 6.1 收敛定义

**定义**：矩阵序列 $\{A_k\}$ **收敛**到 $A$，记为 $\lim_{k \to \infty} A_k = A$，若
$$\lim_{k \to \infty} \|A_k - A\| = 0$$
对某种（等价地，任意）矩阵范数成立。

### 6.2 收敛的等价条件

**定理**：以下等价：
1. $A_k \to A$（按范数收敛）
2. $(A_k)_{ij} \to A_{ij}$（逐元素收敛）
3. $A_k x \to Ax$ 对所有 $x$（逐点收敛）

### 6.3 幂级数

**定理**：矩阵幂级数 $\sum_{k=0}^\infty c_k A^k$ 收敛，若 $\rho(A) < R$（$R$ 是标量幂级数的收敛半径）。

**例**：
- 几何级数：$\sum_{k=0}^\infty A^k = (I - A)^{-1}$，若 $\rho(A) < 1$
- 指数函数：$e^A = \sum_{k=0}^\infty \frac{A^k}{k!}$，对所有 $A$ 收敛

---

## 七、条件数

### 7.1 定义

**定义**：矩阵 $A$（相对于范数 $\|\cdot\|$）的**条件数**定义为
$$\kappa(A) = \|A\| \cdot \|A^{-1}\|$$

### 7.2 2-范数条件数
$$\kappa_2(A) = \|A\|_2 \|A^{-1}\|_2 = \frac{\sigma_{\max}(A)}{\sigma_{\min}(A)}$$

### 7.3 条件数的意义

**线性方程组的敏感性**：设 $Ax = b$，若 $b$ 有扰动 $\delta b$，则解的扰动 $\delta x$ 满足
$$\frac{\|\delta x\|}{\|x\|} \leq \kappa(A) \frac{\|\delta b\|}{\|b\|}$$

**结论**：条件数越大，线性方程组越"病态"。

### 7.4 性质

1. $\kappa(A) \geq 1$
2. $\kappa(A) = \kappa(A^{-1})$
3. $\kappa(\alpha A) = \kappa(A)$
4. $\kappa(AB) \leq \kappa(A)\kappa(B)$
5. 对酉矩阵 $U$：$\kappa(U) = 1$

### 7.5 完整例题：对角矩阵的范数与条件数

设
$$A = \begin{pmatrix}4 & 0 \\ 0 & \frac{1}{2}\end{pmatrix}$$

则 1-范数、无穷范数和 Frobenius 范数分别为
$$\|A\|_1 = \max\{4, 1/2\} = 4$$
$$\|A\|_\infty = \max\{4, 1/2\} = 4$$
$$\|A\|_F = \sqrt{4^2 + (1/2)^2} = \frac{\sqrt{65}}{2}$$

因为 $A$ 是对角矩阵，2-范数就是对角元绝对值的最大值：
$$\|A\|_2 = 4$$

其逆矩阵为
$$A^{-1} = \begin{pmatrix}\frac{1}{4} & 0 \\ 0 & 2\end{pmatrix}$$

因此
$$\|A^{-1}\|_2 = 2, \qquad \kappa_2(A) = 4 \times 2 = 8$$

**直观理解**：这个矩阵把一个方向放大 4 倍，另一个方向缩小到原来的 1/2 倍，所以它对不同方向的“拉伸不均匀”。条件数 8 正是在量化这种不均匀性和对扰动的敏感程度。

---

## 重要公式汇总

| 名称 | 公式 |
|------|------|
| 谱范数 | $\|A\|_2 = \sigma_{\max}(A)$ |
| Frobenius范数 | $\|A\|_F = \sqrt{\text{tr}(A^*A)}$ |
| 谱半径与范数 | $\rho(A) \leq \|A\|$ |
| 谱半径极限 | $\rho(A) = \lim \|A^k\|^{1/k}$ |
| 条件数 | $\kappa_2(A) = \frac{\sigma_{\max}}{\sigma_{\min}}$ |

---

## 总结

- 矩阵范数把矩阵的大小、误差传播和收敛性统一到可比较的数值语言里。
- 诱导范数和谱范数是最常用的矩阵分析工具，和奇异值密切相关。
- 条件数刻画了线性系统对扰动的敏感程度，是数值线性代数的核心指标。

## 易错点

- 把任意矩阵范数和诱导范数混为一谈。
- 忽略次乘性与一致性之间的区别。
- 误把谱范数当成逐元素最大值。
- 忘记条件数只对可逆矩阵定义更有意义。

## 练习（分层）

### A档（基础）

1. 判断一个给定函数是否满足范数公理。
2. 计算一个矩阵的 Frobenius 范数。
3. 求一个简单矩阵的 1-范数和无穷范数。
4. 验证矩阵范数的次乘性。

### B档（进阶）

1. 证明一个矩阵范数是诱导范数。
2. 计算一个矩阵的谱范数。
3. 比较两个不同矩阵范数的大小关系。
4. 估计线性方程组误差与条件数的关系。

### C档（挑战）

1. 证明所有有限维空间上的范数等价。
2. 证明谱范数等于最大奇异值。
3. 推导谱半径与矩阵范数的界。
4. 分析一个病态矩阵的条件数并解释其数值意义。

## 相关链接

- [[07_Rank|矩阵的秩]] - 奇异值与秩的关系
- [[21_SVD|奇异值分解]] - 谱范数与奇异值
- [[07_矩阵函数]] - 矩阵指数、矩阵幂级数

---

**创建时间**：2026年4月3日
**所属模块**：高等代数 → 矩阵分析
