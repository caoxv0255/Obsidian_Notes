---
type: note
subject: abstract_algebra
chapter: 01
created: 2026-04-03
status: complete
updated: 2026-04-27
---

# 群的定义与基本性质

## 📌 学习目标

- 理解群、Abel 群、子群、元素阶与循环群的定义和典型例子
- 掌握单位元唯一性、逆元唯一性、消去律和子群判别定理
- 能用同构和群表描述简单群结构

## ✅ 先修

- [[../00_Symbols_Conventions|符号与约定总表]]

## 难度分层

- **基础**：群定义、基本例子、群的基本性质
- **进阶**：子群判别、元素阶、循环群与同构
- **拓展**：群表、Cayley 定理、Klein 四元群

## 自测（3问速测）

1. 群的四条公理分别是什么？
2. 为什么单位元和逆元都是唯一的？
3. 为什么循环群的子群仍然是循环群？

## 一、群的定义

### 1.1 定义

**定义**：一个**群**是一个集合 $G$ 配合一个二元运算 $\cdot: G \times G \to G$，满足以下四条公理：

1. **封闭性**：对任意 $a, b \in G$，$a \cdot b \in G$
2. **结合律**：$(a \cdot b) \cdot c = a \cdot (b \cdot c)$
3. **单位元**：存在 $e \in G$，使得 $e \cdot a = a \cdot e = a$ 对所有 $a \in G$
4. **逆元**：对任意 $a \in G$，存在 $a^{-1} \in G$ 使得 $a \cdot a^{-1} = a^{-1} \cdot a = e$

### 1.2 群的例子

**例1：数群**
- $(\mathbb{Z}, +)$：整数加法群
- $(\mathbb{Q}, +)$：有理数加法群
- $(\mathbb{R}, +)$：实数加法群
- $(\mathbb{R}^*, \cdot)$：非零实数乘法群

**例2：矩阵群**
- $GL_n(\mathbb{R})$：$n$ 阶实可逆矩阵群（一般线性群）
- $SL_n(\mathbb{R})$：行列式为1的矩阵群（特殊线性群）
- $O(n)$：正交矩阵群
- $SO(n)$：行列式为1的正交矩阵群（特殊正交群）

**例3：对称群**
- $S_n$：$n$ 个元素的置换群
- $D_n$：正 $n$ 边形的对称群（二面体群）

### 1.3 Abel群

**定义**：若群 $G$ 满足交换律 $a \cdot b = b \cdot a$，则称 $G$ 为**Abel群**（或交换群）。

**例**：$(\mathbb{Z}, +)$ 是Abel群，$S_n$（$n \geq 3$）不是Abel群。

---

## 二、群的基本性质

### 2.1 单位元的唯一性

**定理**：群的单位元唯一。

**证明**：设 $e, e'$ 都是单位元，则 $e = e \cdot e' = e'$。

### 2.2 逆元的唯一性

**定理**：群中每个元素的逆元唯一。

**证明**：设 $b, c$ 都是 $a$ 的逆元，则
$$b = b \cdot e = b \cdot (a \cdot c) = (b \cdot a) \cdot c = e \cdot c = c$$

### 2.3 消去律

**定理**：在群中，
- 若 $ab = ac$，则 $b = c$
- 若 $ba = ca$，则 $b = c$

**证明**：$ab = ac \Rightarrow a^{-1}ab = a^{-1}ac \Rightarrow b = c$。

### 2.4 穿脱公式

**定理**：$(ab)^{-1} = b^{-1}a^{-1}$

**证明**：$(ab)(b^{-1}a^{-1}) = a(bb^{-1})a^{-1} = aea^{-1} = aa^{-1} = e$。

---

## 三、子群

### 3.1 定义

**定义**：设 $G$ 是群，$H \subseteq G$。若 $H$ 在 $G$ 的运算下也构成群，则称 $H$ 是 $G$ 的**子群**，记作 $H \leq G$。

### 3.2 子群判别定理

**定理**：设 $G$ 是群，$H$ 是 $G$ 的非空子集。则 $H \leq G$ 当且仅当：
- 对任意 $a, b \in H$，有 $ab \in H$
- 对任意 $a \in H$，有 $a^{-1} \in H$

**等价形式**：$H \leq G$ 当且仅当对任意 $a, b \in H$，有 $ab^{-1} \in H$。

### 3.3 子群的例子

**例1**：$n\mathbb{Z} = \{nk : k \in \mathbb{Z}\}$ 是 $\mathbb{Z}$ 的子群

**例2**：$SL_n(\mathbb{R})$ 是 $GL_n(\mathbb{R})$ 的子群

**例3**：$A_n$（交替群）是 $S_n$ 的子群

### 3.4 特殊子群

**中心**：$Z(G) = \{g \in G : gh = hg, \forall h \in G\}$

**中心化子**：$C_G(a) = \{g \in G : ga = ag\}$

**正规化子**：$N_G(H) = \{g \in G : gH = Hg\}$

---

## 四、群的阶

### 4.1 定义

**定义**：群 $G$ 的**阶**是其元素个数，记为 $|G|$。

- **有限群**：$|G| < \infty$
- **无限群**：$|G| = \infty$

### 4.2 元素的阶

**定义**：元素 $g \in G$ 的**阶**是使 $g^n = e$ 的最小正整数 $n$。若不存在这样的 $n$，则阶为无穷。

**记号**：$o(g)$ 或 $|g|$

### 4.3 性质

**定理**：设 $G$ 是有限群，$|G| = n$，则对任意 $g \in G$：
1. $o(g)$ 整除 $n$
2. $g^n = e$

**证明**：由Lagrange定理（后续章节）。

---

## 五、循环群

### 5.1 定义

**定义**：若群 $G$ 由一个元素生成，即 $G = \langle g \rangle = \{g^n : n \in \mathbb{Z}\}$，则称 $G$ 为**循环群**。

### 5.2 循环群的结构

**定理**：
- 无限循环群同构于 $(\mathbb{Z}, +)$
- $n$ 阶有限循环群同构于 $(\mathbb{Z}_n, +)$

### 5.3 循环群的子群

**定理**：循环群的子群都是循环群。

**定理**：设 $G = \langle g \rangle$ 是 $n$ 阶循环群，则对 $n$ 的每个正因子 $d$，$G$ 恰有一个 $d$ 阶子群 $\langle g^{n/d} \rangle$。

### 5.4 生成元

**定理**：$n$ 阶循环群的生成元恰有 $\varphi(n)$ 个，其中 $\varphi$ 是Euler函数。

**完整例题：在循环群里找子群**

设 $G = (\mathbb{Z}_{12}, +)$，取子集
$$H = \{0, 4, 8\}$$

检查子群条件：
- $0 \in H$，故 $H$ 非空
- 在模 12 加法下，$4 + 4 = 8$，$8 + 4 = 0$，$8 + 8 = 4$
- 每个元素的逆元也都在 $H$ 中：$-4 \equiv 8 \pmod{12}$，$-8 \equiv 4 \pmod{12}$

因此 $H \leq \mathbb{Z}_{12}$。
进一步看，$H = \langle 4 \rangle$，所以 $4$ 的阶是 3。

**直观理解**：循环群的子群可以理解成“固定步长反复走出来的点集”。在这里每次加 4，只会在 $0, 4, 8$ 之间循环，形成一个 3 阶子群。

---

## 六、群的同构

### 6.1 定义

**定义**：设 $G, H$ 是群。映射 $f: G \to H$ 称为**同构**，若：
1. $f$ 是双射
2. $f(ab) = f(a)f(b)$（保持运算）

若存在同构 $f: G \to H$，则称 $G$ 与 $H$ **同构**，记为 $G \cong H$。

### 6.2 同构的性质

**定理**：同构保持群的结构：
- $f(e_G) = e_H$
- $f(a^{-1}) = f(a)^{-1}$
- $o(f(a)) = o(a)$
- $f(G) \cong G$

### 6.3 Cayley定理

**定理**：每个群同构于某个置换群的子群。

**证明**：对 $g \in G$，定义左乘映射 $\lambda_g: G \to G$，$\lambda_g(x) = gx$。则 $\{\lambda_g : g \in G\}$ 构成 $G$ 上置换群的子群，同构于 $G$。

---

## 七、群表（乘法表）

### 7.1 定义

有限群的运算可以用**群表**（或Cayley表）表示：

$$
\begin{array}{c|cccc}
\cdot & e & a & b & c \\
\hline
e & e & a & b & c \\
a & a & ? & ? & ? \\
b & b & ? & ? & ? \\
c & c & ? & ? & ?
\end{array}
$$

### 7.2 群表的性质

- 每行每列包含群的每个元素恰好一次（拉丁方性质）
- 由消去律保证

### 7.3 Klein四元群

$V_4 = \{e, a, b, c\}$，运算表：
$$
\begin{array}{c|cccc}
\cdot & e & a & b & c \\
\hline
e & e & a & b & c \\
a & a & e & c & b \\
b & b & c & e & a \\
c & c & b & a & e
\end{array}
$$

$V_4 \cong \mathbb{Z}_2 \times \mathbb{Z}_2$

---

## 重要概念汇总

| 概念 | 定义 |
|------|------|
| 群 | 有单位元、逆元的代数结构 |
| Abel群 | 满足交换律的群 |
| 子群 | 对运算封闭的子集 |
| 阶 | 元素个数 / 元素的周期 |
| 循环群 | 由一个元素生成的群 |
| 同构 | 保持运算的双射 |

---

## 总结

- 群是抽象代数的起点，核心是“运算封闭且可逆”。
- 子群判别、元素阶和循环群是后续章节的基础工具。
- 同构说明“结构相同但表示不同”，是代数思维的关键。

## 易错点

- 把“集合 + 二元运算”直接当成群，忘记检查四条公理。
- 把阿贝尔群和一般群混淆，误以为交换律总是成立。
- 计算元素阶时忘记它是满足 $g^n=e$ 的最小正整数。
- 讨论循环群子群时忘记“每个正因子只对应一个子群”。

## 练习（分层）

### A档（基础）

1. 判断 $(\mathbb{Z}, +)$、$(\mathbb{N}, +)$、$(\mathbb{R}^*, \cdot)$ 是否为群。
2. 证明群的单位元唯一。
3. 证明群中每个元素的逆元唯一。
4. 计算 $S_3$ 中某个置换的阶。

### B档（进阶）

1. 用子群判别定理判断 $n\mathbb{Z}$ 是否为 $\mathbb{Z}$ 的子群。
2. 证明循环群的子群都是循环群。
3. 说明 $\mathbb{Z}_n$ 的生成元个数与 Euler 函数的关系。
4. 写出 $D_4$ 的一个子群并说明它为什么是子群。

### C档（挑战）

1. 证明 Cayley 定理。
2. 利用群表验证 $V_4 \cong \mathbb{Z}_2 \times \mathbb{Z}_2$。
3. 研究 $S_3$ 与 $D_3$ 的同构关系。
4. 证明：有限群中元素的阶整除群的阶（引用 Lagrange 定理的结论即可）。

## 相关链接

- [[02_陪集与商群]] - Lagrange定理与商群
- [[03_群的结构]] - 循环群、置换群
- [[05_线性变换的深入理论|线性变换]] - 变换群

---

**创建时间**：2026年4月3日
**所属模块**：近世代数 → 群论基础

