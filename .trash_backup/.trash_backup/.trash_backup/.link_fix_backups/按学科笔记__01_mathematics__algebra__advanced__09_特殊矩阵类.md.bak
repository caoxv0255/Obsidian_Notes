---
type: note
subject: advanced_algebra
chapter: 09
created: 2026-04-03
updated: 2026-04-24
status: complete
---

# 特殊矩阵类

## 📌 学习目标

- 理解正定、正交、投影、幂零、幂等和对角占优矩阵的判别与性质
- 掌握特殊矩阵和矩阵分解、谱性质之间的联系
- 能用这些矩阵类型分析稳定性、可逆性和几何结构

## ✅ 先修

- [[15_Diagonalization|对角化]]
- [[18_Inner_Product|内积空间]]
- [[10_矩阵分解进阶]]
- [[../00_Symbols_Conventions|符号与约定总表]]

## 难度分层

- **基础**：正定矩阵、投影矩阵、幂零矩阵
- **进阶**：Gershgorin 圆盘、对角占优、三对角矩阵
- **拓展**：Toeplitz/Hankel 矩阵、Jordan-Chevalley 分解

## 自测（3问速测）

1. 正定矩阵为什么一定可逆？
2. 幂零矩阵和幂等矩阵的谱性质有什么不同？
3. Gershgorin 圆盘定理能提供什么信息？

## 一、正定矩阵

### 1.1 定义

**定义**：实对称矩阵 $A$ 称为**正定的**，若对任意非零向量 $x$，有 $x^T A x > 0$。

类似定义：
- **半正定**：$x^T A x \geq 0$
- **负定**：$x^T A x < 0$（$x \neq 0$）
- **不定**：$x^T A x$ 可正可负

### 1.2 等价条件

**定理**：对实对称矩阵 $A$，以下等价：
1. $A$ 正定
2. 所有特征值为正
3. 所有顺序主子式为正（Sylvester准则）
4. 存在可逆矩阵 $P$ 使得 $A = P^T P$
5. $A = B^T B$ 其中 $B$ 列满秩

### 1.3 性质

1. 若 $A, B$ 正定，则 $A + B$ 正定
2. 若 $A$ 正定，则 $A^{-1}$ 正定
3. 若 $A$ 正定，则 $\det A > 0$
4. 正定矩阵可开平方：存在唯一正定 $B$ 使 $B^2 = A$
5. 正定矩阵的迹和行列式均为正

### 1.4 正定性的判定

**Sylvester准则**：$A$ 正定 $\Leftrightarrow$ 所有顺序主子式 $\Delta_k > 0$

**例**：$\begin{pmatrix} a & b \\ b & c \end{pmatrix}$ 正定 $\Leftrightarrow$ $a > 0$ 且 $ac - b^2 > 0$

---

## 二、正规矩阵

### 2.1 定义

**定义**：矩阵 $A$ 称为**正规的**，若 $A^* A = A A^*$。

### 2.2 正规矩阵的例子

- Hermite矩阵：$A^* = A$
- 反Hermite矩阵：$A^* = -A$
- 酉矩阵：$A^* A = I$
- 实对称矩阵：$A^T = A$
- 实反对称矩阵：$A^T = -A$
- 正交矩阵：$A^T A = I$

### 2.3 正规矩阵的谱定理

**定理**：$A$ 正规 $\Leftrightarrow$ $A$ 可酉对角化

即：$A = U \Lambda U^*$，其中 $U$ 酉矩阵，$\Lambda$ 对角矩阵。

### 2.4 性质

**定理**：$A$ 正规 $\Leftrightarrow$ $\|Ax\| = \|A^*x\|$ 对所有 $x$

**推论**：正规矩阵属于不同特征值的特征向量正交。

---

## 三、投影矩阵

### 3.1 定义

**定义**：矩阵 $P$ 称为**投影矩阵**（或幂等矩阵），若 $P^2 = P$。

### 3.2 几何意义

若 $P$ 是投影矩阵，则：
- $\text{Im}\, P = \{x : Px = x\}$（不动点集）
- $\ker P = \{x : Px = 0\}$
- $V = \text{Im}\, P \oplus \ker P$

$P$ 将向量投影到 $\text{Im}\, P$，沿 $\ker P$ 方向。

### 3.3 正交投影

**定义**：投影矩阵 $P$ 称为**正交投影**，若 $P = P^*$（Hermite）。

**等价条件**：
- $P^2 = P$ 且 $P^* = P$
- $\|Px - x\| = \min_{y \in \text{Im}\,P} \|y - x\|$

### 3.4 性质

1. 投影矩阵的特征值为 $0$ 或 $1$
2. $\text{tr}(P) = \text{rank}(P)$
3. $I - P$ 也是投影矩阵，投影到 $\ker P$
4. 若 $P, Q$ 是投影矩阵且 $PQ = QP = 0$，则 $P + Q$ 也是投影矩阵

---

## 四、幂等矩阵

### 4.1 定义

幂等矩阵即投影矩阵，$A^2 = A$。

### 4.2 Jordan标准形

**定理**：幂等矩阵可对角化，且特征值为 $0$ 或 $1$。

**证明**：极小多项式 $m_A(x)$ 整除 $x^2 - x = x(x-1)$，故无重根。

### 4.3 与子空间的关系

每个幂等矩阵 $P$ 对应一个直和分解 $V = \text{Im}\, P \oplus \ker P$。

反之，给定直和分解 $V = W_1 \oplus W_2$，存在唯一幂等矩阵 $P$ 使 $\text{Im}\,P = W_1$，$\ker P = W_2$。

---

## 五、幂零矩阵

### 5.1 定义

**定义**：矩阵 $A$ 称为**幂零的**，若存在正整数 $k$ 使 $A^k = 0$。最小的这样的 $k$ 称为**幂零指数**。

### 5.2 性质

1. 幂零矩阵的所有特征值为 $0$
2. 幂零矩阵的迹为 $0$
3. $\det A = 0$
4. $I - A$ 可逆，$(I - A)^{-1} = I + A + A^2 + \cdots + A^{k-1}$

### 5.3 Jordan标准形

**定理**：幂零矩阵的Jordan标准形由若干Jordan块 $J_k(0)$ 组成。

### 5.4 幂零分解

**定理**（Jordan-Chevalley分解）：任意矩阵 $A$ 可唯一分解为
$$A = D + N$$
其中 $D$ 可对角化，$N$ 幂零，且 $DN = ND$。

---

## 六、对角占优矩阵

### 6.1 定义

**定义**：矩阵 $A$ 称为（行）**严格对角占优**，若
$$|a_{ii}| > \sum_{j \neq i} |a_{ij}|$$
对所有 $i$ 成立。

### 6.2 Gershgorin圆盘定理

**定理**：矩阵 $A$ 的每个特征值至少落在一个Gershgorin圆盘
$$D_i = \{z \in \mathbb{C} : |z - a_{ii}| \leq \sum_{j \neq i} |a_{ij}|\}$$
内。

**推论**：严格对角占优矩阵可逆。

### 6.3 对角占优正定矩阵

**定理**：若 $A$ 实对称、对角元为正、严格对角占优，则 $A$ 正定。

---

## 七、三对角矩阵

### 7.1 定义

**定义**：矩阵 $A$ 称为**三对角矩阵**，若 $a_{ij} = 0$ 当 $|i - j| > 1$。

形式：
$$\begin{pmatrix} a_1 & b_1 & & & \\ c_1 & a_2 & b_2 & & \\ & c_2 & a_3 & \ddots & \\ & & \ddots & \ddots & b_{n-1} \\ & & & c_{n-1} & a_n \end{pmatrix}$$

### 7.2 行列式的递推

设 $D_n$ 是 $n$ 阶三对角矩阵的行列式，则
$$D_n = a_n D_{n-1} - b_{n-1} c_{n-1} D_{n-2}$$

### 7.3 特征值

对称三对角矩阵的特征值可用二分法或QR算法高效计算。

---

## 八、Hankel与Toeplitz矩阵

### 8.1 Toeplitz矩阵

**定义**：对角线上元素相同的矩阵
$$\begin{pmatrix} a_0 & a_1 & a_2 & \cdots \\ a_{-1} & a_0 & a_1 & \cdots \\ a_{-2} & a_{-1} & a_0 & \cdots \\ \vdots & \vdots & \vdots & \ddots \end{pmatrix}$$

**性质**：矩阵向量乘法可用FFT加速（$O(n \log n)$ 而非 $O(n^2)$）

### 8.2 Hankel矩阵

**定义**：反对角线上元素相同的矩阵
$$\begin{pmatrix} a_0 & a_1 & a_2 & \cdots \\ a_1 & a_2 & a_3 & \cdots \\ a_2 & a_3 & a_4 & \cdots \\ \vdots & \vdots & \vdots & \ddots \end{pmatrix}$$

**应用**：系统辨识、矩问题

---

## 特殊矩阵性质汇总

| 矩阵类型 | 定义条件 | 关键性质 |
|----------|----------|----------|
| 正定 | $x^T A x > 0$ | 特征值全正，可开平方 |
| 正规 | $A^* A = A A^*$ | 可酉对角化 |
| 投影 | $P^2 = P$ | 特征值为0或1 |
| 正交投影 | $P^2 = P$, $P^* = P$ | 最小距离投影 |
| 幂零 | $A^k = 0$ | 特征值全为0 |
| 幂等 | $A^2 = A$ | 可对角化 |
| 对角占优 | $|a_{ii}| > \sum_{j \neq i} |a_{ij}|$ | 可逆 |

---

## 相关链接

- [[15_Diagonalization|对角化]] - 正规矩阵的对角化
- [[18_Inner_Product|内积空间]] - 正交投影
- [[10_矩阵分解进阶]] - Cholesky分解与正定矩阵

## 总结

- 特殊矩阵类把谱性质、几何意义和数值算法连接成一个统一框架。
- 正定、投影、幂零和对角占优矩阵分别对应不同的结构判定。
- 三对角、Toeplitz 和 Hankel 矩阵则体现了稀疏结构与快速算法的结合。

## 易错点

- 把正定矩阵与所有特征值非负的半正定矩阵混淆。
- 以为幂零矩阵一定可逆，实际上恰好相反。
- 把幂等矩阵和投影矩阵完全等同，忽略是否正交投影。
- 忽略对角占优条件通常是充分而非必要条件。

## 练习（分层）

### A档（基础）

1. 判断一个对称矩阵是否正定。
2. 计算一个幂等矩阵的特征值。
3. 判断一个矩阵是否幂零。
4. 检查一个矩阵是否严格对角占优。

### B档（进阶）

1. 用 Sylvester 准则判断正定性。
2. 构造一个投影矩阵并写出其像与核。
3. 使用 Gershgorin 圆盘估计特征值范围。
4. 计算一个三对角矩阵的行列式递推。

### C档（挑战）

1. 证明幂零矩阵的 Jordan 结构。
2. 证明正定矩阵存在 Cholesky 分解。
3. 分析一个 Toeplitz 矩阵的快速乘法结构。
4. 证明 Jordan-Chevalley 分解的唯一性思路。

---

**创建时间**：2026年4月3日
**所属模块**：高等代数 → 特殊矩阵与分解
