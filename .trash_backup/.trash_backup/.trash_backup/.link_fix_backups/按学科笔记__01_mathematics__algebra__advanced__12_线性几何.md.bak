---
type: note
subject: advanced_algebra
chapter: 12
created: 2026-04-03
updated: 2026-04-24
status: complete
---

# 线性几何

## 📌 学习目标

- 理解仿射空间、二次型和几何变换之间的联系
- 掌握正交、仿射与射影变换的几何不变量
- 能用代数方法描述几何对象和二次曲面分类

## ✅ 先修

- [[10_矩阵分解进阶]]
- [[09_特殊矩阵类]]
- [[../Linear/17_Quadratic_Forms|二次型]]
- [[../Linear/18_Inner_Product|内积空间]]

## 难度分层

- **基础**：仿射空间、仿射子空间、正交变换
- **进阶**：二次型、惯性定理、几何意义
- **拓展**：变换群、Erlangen 纲领、二次曲面分类

## 自测（3问速测）

1. 仿射空间和向量空间最大的区别是什么？
2. 为什么二次型可以分类出椭球、双曲面等几何对象？
3. Erlangen 纲领的核心思想是什么？

## 一、仿射空间

### 1.1 定义

**定义**：**仿射空间**是一个点集 $A$，配合向量空间 $V$，使得：
1. 对任意 $p, q \in A$，存在唯一向量 $v \in V$，记为 $\overrightarrow{pq}$
2. 对任意 $p \in A$ 和 $v \in V$，存在唯一 $q \in A$ 使 $\overrightarrow{pq} = v$
3. $\overrightarrow{pq} + \overrightarrow{qr} = \overrightarrow{pr}$（平行四边形法则）

**直观**：仿射空间是"忘记原点"的向量空间。

### 1.2 仿射子空间

**定义**：仿射空间 $A$ 的子集 $B$ 称为**仿射子空间**，若存在点 $p \in B$ 和向量子空间 $W \subseteq V$ 使得
$$B = \{p + w : w \in W\}$$

**维数**：$\dim B = \dim W$

### 1.3 仿射组合与仿射包

**仿射组合**：点 $p_1, \ldots, p_k$ 的仿射组合是 $\sum_i \lambda_i p_i$，其中 $\sum_i \lambda_i = 1$

**仿射包**：$\text{aff}(S)$ 是包含 $S$ 的最小仿射子空间

### 1.4 仿射变换

**定义**：仿射空间之间的映射 $f: A \to B$ 称为**仿射变换**，若
$$f(p + v) = f(p) + L(v)$$
其中 $L: V \to W$ 是线性映射。

**矩阵表示**：在齐次坐标下，仿射变换表示为
$$\begin{pmatrix} L & t \\ 0 & 1 \end{pmatrix}$$

---

## 二、射影空间

### 2.1 定义

**定义**：$n$ 维射影空间 $\mathbb{P}^n$ 是 $\mathbb{R}^{n+1} \setminus \{0\}$ 的等价类，其中 $x \sim \lambda x$（$\lambda \neq 0$）。

**记号**：$[x_0 : x_1 : \cdots : x_n]$ 称为**齐次坐标**

### 2.2 几何解释

$\mathbb{P}^2$ 可以理解为：
- 过原点的直线
- 球面 $S^2$ 的对径点等同
- 平面加上"无穷远点"

### 2.3 仿射坐标与齐次坐标

**对应关系**：
$$[x_0 : x_1 : \cdots : x_n] \mapsto \left(\frac{x_1}{x_0}, \ldots, \frac{x_n}{x_0}\right)$$
当 $x_0 \neq 0$。

$x_0 = 0$ 对应"无穷远点"。

### 2.4 射影变换

**定义**：射影变换是齐次坐标下的线性变换：
$$[x] \mapsto [Tx]$$
其中 $T \in GL(n+1)$。

**性质**：射影变换保持交叉比：
$$\frac{d_{13} \cdot d_{24}}{d_{14} \cdot d_{23}}$$
在射影变换下不变。

---

## 三、二次曲面

### 3.1 二次曲面的标准形

**定理**：实二次曲面可通过正交变换化为标准形：

| 名称 | 标准方程 | 类型 |
|------|----------|------|
| 椭球面 | $\frac{x^2}{a^2} + \frac{y^2}{b^2} + \frac{z^2}{c^2} = 1$ | 椭圆型 |
| 单叶双曲面 | $\frac{x^2}{a^2} + \frac{y^2}{b^2} - \frac{z^2}{c^2} = 1$ | 双曲型 |
| 双叶双曲面 | $\frac{x^2}{a^2} + \frac{y^2}{b^2} - \frac{z^2}{c^2} = -1$ | 双曲型 |
| 椭圆抛物面 | $\frac{x^2}{a^2} + \frac{y^2}{b^2} = z$ | 抛物型 |
| 双曲抛物面 | $\frac{x^2}{a^2} - \frac{y^2}{b^2} = z$ | 抛物型 |
| 二次锥面 | $\frac{x^2}{a^2} + \frac{y^2}{b^2} - \frac{z^2}{c^2} = 0$ | 锥型 |

### 3.2 分类方法

二次曲面由二次型 $x^T A x + b^T x + c = 0$ 定义：

1. 将 $A$ 对角化，消去交叉项
2. 平移消去一次项（若可能）
3. 识别曲面类型

### 3.3 二次曲面的不变量

**定理**：二次曲面的类型由以下不变量决定：
- $I_1 = \text{tr}(A)$
- $I_2 = \det(A)$
- $I_3$ = 特征值的符号

---

## 四、几何变换群

### 4.1 变换群的概念

**定义**：几何变换构成群：
- 恒等变换是单位元
- 变换的复合是群运算
- 每个变换有逆

### 4.2 重要变换群

| 群名称 | 变换类型 | 不变量 |
|--------|----------|--------|
| $O(n)$ | 正交变换 | 距离 |
| $SO(n)$ | 特殊正交变换 | 距离、定向 |
| $E(n)$ | 等距变换 | 距离 |
| $GL(n)$ | 可逆线性变换 | 无 |
| $SL(n)$ | 特殊线性变换 | 体积比 |
| $Aff(n)$ | 仿射变换 | 平行性、面积比 |
| $PGL(n)$ | 射影变换 | 交叉比 |

### 4.3 Klein的Erlangen纲领

**核心思想**：几何学研究群作用下的不变性质。

- 欧氏几何：研究等距变换群的不变量
- 仿射几何：研究仿射变换群的不变量
- 射影几何：研究射影变换群的不变量

---

## 五、线性变换的几何意义

### 5.1 特征向量的几何意义

特征向量 $v$ 满足 $Av = \lambda v$，即：
- $A$ 将 $v$ 映射为与 $v$ 共线的向量
- $\lambda$ 是伸缩因子

### 5.2 奇异值的几何意义

设 $A = U\Sigma V^*$ 是SVD：
- $V^*$ 将单位球映为另一椭球
- $\Sigma$ 沿各轴伸缩（奇异值是伸缩因子）
- $U$ 旋转到最终位置

**结论**：线性变换将单位球映为椭球，奇异值是椭球半轴长度。

### 5.3 行列式的几何意义

**定理**：$|\det A|$ 是单位超立方体在 $A$ 下的像的体积。

**符号**：$\det A > 0$ 保持定向，$\det A < 0$ 反转定向。

---

## 六、二次型与几何

### 6.1 二次型的几何意义

二次型 $Q(x) = x^T A x$ 定义了超曲面：
- $Q(x) = 1$：二次曲面
- $Q(x) = 0$：二次锥面

### 6.2 惯性定理

**定理（Sylvester惯性定理）**：实二次型在合同变换下，正、负、零惯性指数不变。

即：标准形中正、负、零系数的个数是合同不变量。

### 6.3 正定性与几何

| 类型 | 条件 | 几何意义 |
|------|------|----------|
| 正定 | $x^T Ax > 0$ ($x \neq 0$) | 椭球面 $x^T Ax = 1$ |
| 负定 | $x^T Ax < 0$ ($x \neq 0$) | 椭球面 $x^T Ax = -1$ |
| 不定 | 有正有负 | 双曲面 |

---

## 七、线性空间与几何的联系

### 7.1 子空间与几何

| 代数概念 | 几何对象 |
|----------|----------|
| 向量子空间 | 过原点的平面 |
| 仿射子空间 | 任意平面 |
| 正交补 | 垂直平面 |
| 商空间 | 平行平面族 |

### 7.2 线性映射与几何变换

| 代数概念 | 几何变换 |
|----------|----------|
| 正交矩阵 | 旋转/反射 |
| 对称矩阵 | 主轴方向的伸缩 |
| 正定矩阵 | 各方向的伸缩 |
| 幂等矩阵 | 投影 |

---

## 重要定理汇总

| 定理 | 内容 |
|------|------|
| 二次曲面分类 | 可化为标准形（椭球/双曲/抛物等） |
| 惯性定理 | 正负惯性指数合同不变 |
| Erlangen纲领 | 几何 = 群 + 不变量 |
| 行列式几何意义 | 有向体积比 |

---

## 总结

- 线性几何把代数对象和几何对象通过变换群和不变量统一起来。
- 仿射空间、正交变换和二次型是连接几何直觉与线性代数的关键语言。
- Erlangen 纲领提供了从“对称性”理解几何的总框架。

## 易错点

- 把仿射空间误认为带原点的向量空间。
- 混淆正定、半正定和不定的几何意义。
- 忽略合同变换和相似变换的区别。
- 以为几何分类只依赖特征值，而忽略合同不变量。

## 练习（分层）

### A档（基础）

1. 判断一个集合是否构成仿射子空间。
2. 写出一个正交变换矩阵。
3. 计算一个二次型的标准形。
4. 判断一个对称矩阵对应的二次曲面类型。

### B档（进阶）

1. 用合同变换化简一个二次型。
2. 证明惯性定理的一个具体例子。
3. 分析一个仿射变换的不变量。
4. 求一个几何变换群下的不变量。

### C档（挑战）

1. 证明一个二次曲面的分类结论。
2. 分析一个射影变换的交叉比不变量。
3. 用群作用解释一个几何分类问题。
4. 从 Erlangen 纲领角度重述一类几何命题。

## 相关链接

- [[17_Quadratic_Forms|二次型]] - 二次型的代数理论
- [[20_Orthogonal_Transformations|正交变换]] - 几何变换的代数表示
- [[01_群的定义与基本性质|群论]] - 变换群的抽象理论

## 总结

- 线性几何把代数对象和几何对象通过变换群和不变量统一起来。
- 仿射空间、正交变换和二次型是连接几何直觉与线性代数的关键语言。
- Erlangen 纲领提供了从“对称性”理解几何的总框架。

## 易错点

- 把仿射空间误认为带原点的向量空间。
- 混淆正定、半正定和不定的几何意义。
- 忽略合同变换和相似变换的区别。
- 以为几何分类只依赖特征值，而忽略合同不变量。

## 练习（分层）

### A档（基础）

1. 判断一个集合是否构成仿射子空间。
2. 写出一个正交变换矩阵。
3. 计算一个二次型的标准形。
4. 判断一个对称矩阵对应的二次曲面类型。

### B档（进阶）

1. 用合同变换化简一个二次型。
2. 证明惯性定理的一个具体例子。
3. 分析一个仿射变换的不变量。
4. 求一个几何变换群下的不变量。

### C档（挑战）

1. 证明一个二次曲面的分类结论。
2. 分析一个射影变换的交叉比不变量。
3. 用群作用解释一个几何分类问题。
4. 从 Erlangen 纲领角度重述一类几何命题。

---

**创建时间**：2026年4月3日
**所属模块**：高等代数 → 线性几何
