---
type: note
subject: advanced_algebra
chapter: 05
created: 2026-04-03
updated: 2026-04-24
status: complete
---

# 线性变换的深入理论

## 📌 学习目标

- 理解不变子空间、谱分解与矩阵分块简化的联系
- 掌握多项式作用于线性算子、Cayley-Hamilton 定理与极小多项式
- 认识正规算子、谱定理与广义特征向量分解的核心结论

## ✅ 先修

- [[23_Linear_Maps|线性映射]]
- [[13_Eigenvalues|特征值与特征向量]]
- [[18_Inner_Product|内积空间]]
- [[../00_Symbols_Conventions|符号与约定总表]]

## 难度分层

- **基础**：不变子空间、矩阵分块、特征子空间
- **进阶**：Cayley-Hamilton、极小多项式、Jordan 相关分解
- **拓展**：正规算子、谱定理、广义特征向量

## 自测（3问速测）

1. 什么是不变子空间，它为什么能帮助简化矩阵？
2. Cayley-Hamilton 定理说明了什么算子恒等式？
3. 正规算子和谱定理的核心结论是什么？

## 一、不变子空间

### 1.1 定义

**定义**：设 $T: V \to V$ 是线性变换。若子空间 $W \subseteq V$ 满足 $T(W) \subseteq W$，则称 $W$ 是 $T$ 的**不变子空间**。

**例**：
- $\{0\}$ 和 $V$ 是任意线性变换的不变子空间
- $\ker T$ 和 $\text{Im}\, T$ 是 $T$ 的不变子空间
- 特征子空间 $E_\lambda = \ker(T - \lambda I)$ 是不变子空间

### 1.2 不变子空间与矩阵简化

**定理**：若 $V = W_1 \oplus W_2$，其中 $W_1, W_2$ 都是 $T$ 的不变子空间，则 $T$ 在适当基下的矩阵为分块对角形：
$$\begin{pmatrix} A_1 & 0 \\ 0 & A_2 \end{pmatrix}$$

### 1.3 不可约不变子空间

**定义**：不变子空间 $W$ 称为**不可约的**，若 $W$ 没有非平凡的 $T$-不变子空间。

**定理**：有限维空间上的线性变换有非平凡不变子空间当且仅当其极小多项式可约。

---

## 二、极小多项式

### 2.1 定义

**定义**：设 $T: V \to V$ 是线性变换。首一多项式 $m_T(x)$ 称为 $T$ 的**极小多项式**，若：
1. $m_T(T) = 0$
2. 对任意满足 $f(T) = 0$ 的非零多项式 $f$，有 $\deg m_T \le \deg f$

### 2.2 存在性与唯一性

**定理**：极小多项式存在且唯一。

**证明要点**：
- 存在性：由维数有限性，$I, T, T^2, \ldots, T^{n^2}$ 线性相关
- 唯一性：若 $m_1, m_2$ 都是极小多项式，则 $m_1 - m_2$ 次数更低且零化 $T$

### 2.3 极小多项式与特征多项式

**定理**：设 $T$ 的特征多项式为 $\chi_T(x)$，则：
1. $m_T(x) \mid \chi_T(x)$
2. $\chi_T(x) \mid m_T(x)^n$（$n = \dim V$）
3. $m_T(x)$ 与 $\chi_T(x)$ 有相同的不可约因子

### 2.4 计算极小多项式

**方法**：设 $T$ 的特征多项式分解为
$$\chi_T(x) = (x - \lambda_1)^{d_1} \cdots (x - \lambda_k)^{d_k}$$

则极小多项式为
$$m_T(x) = (x - \lambda_1)^{m_1} \cdots (x - \lambda_k)^{m_k}$$

其中 $m_i$ 是使 $\ker(T - \lambda_i I)^{m_i} = \ker(T - \lambda_i I)^{m_i+1}$ 的最小正整数。

---

## 三、Cayley-Hamilton定理

### 3.1 定理陈述

**定理（Cayley-Hamilton）**：设 $\chi_T(x)$ 是线性变换 $T: V \to V$ 的特征多项式，则
$$\chi_T(T) = 0$$

### 3.2 证明

**方法一（计算证明）**：设 $\chi_T(x) = \det(xI - A)$，其中 $A$ 是 $T$ 的矩阵。由伴随矩阵，
$$(xI - A) \text{adj}(xI - A) = \chi_T(x) I$$

将 $A$ 代入 $x$，得 $\chi_T(A) = 0$。（需要验证代换合法性）

**方法二（不变子空间归纳）**：取 $v \neq 0$，设 $k$ 是使 $\{v, Tv, \ldots, T^k v\}$ 线性相关的最小正整数。设 $W = \text{span}\{v, Tv, \ldots, T^{k-1}v\}$，则 $W$ 是不变子空间，在 $W$ 上应用归纳假设。

### 3.3 应用

**求逆矩阵**：若 $\chi_A(x) = x^n + c_{n-1}x^{n-1} + \cdots + c_1 x + c_0$，且 $A$ 可逆（$c_0 \neq 0$），则
$$A^{-1} = -\frac{1}{c_0}(A^{n-1} + c_{n-1}A^{n-2} + \cdots + c_1 I)$$

---

## 四、谱理论初步

### 4.1 谱的定义

**定义**：线性变换 $T: V \to V$ 的**谱**是其特征值的集合，记为 $\sigma(T)$。

$$\sigma(T) = \{\lambda \in F : T - \lambda I \text{ 不可逆}\}$$

### 4.2 谱半径

**定义**：对于复线性空间上的线性变换，**谱半径**定义为
$$\rho(T) = \max\{|\lambda| : \lambda \in \sigma(T)\}$$

**定理**：$\rho(T) = \lim_{k \to \infty} \|T^k\|^{1/k}$

### 4.3 谱分解

**定理（谱分解）**：设 $T$ 有 $k$ 个不同的特征值 $\lambda_1, \ldots, \lambda_k$，则存在投影算子 $P_1, \ldots, P_k$ 满足：
1. $P_i P_j = 0$（$i \neq j$）
2. $P_1 + \cdots + P_k = I$
3. $T = \lambda_1 P_1 + \cdots + \lambda_k P_k$

### 4.4 函数演算

**定义**：若 $f$ 是多项式，定义
$$f(T) = f(\lambda_1) P_1 + \cdots + f(\lambda_k) P_k$$

可以推广到解析函数：$e^T, \sqrt{T}, \log T$ 等。

---

## 五、正规算子与谱定理

### 5.1 定义

**定义**：设 $T$ 是内积空间 $V$ 上的线性算子。
- 若 $TT^* = T^*T$，则称 $T$ 是**正规的**
- 若 $T = T^*$，则称 $T$ 是**自伴的**（Hermitian）
- 若 $TT^* = T^*T = I$，则称 $T$ 是**酉算子**

### 5.2 正规算子的性质

**定理**：$T$ 正规 $\Leftrightarrow$ $\|Tv\| = \|T^*v\|$ 对所有 $v$

**定理**：若 $T$ 正规，则 $T$ 的属于不同特征值的特征向量正交。

### 5.3 谱定理（有限维）

**定理**：设 $T$ 是有限维复内积空间上的正规算子，则存在由 $T$ 的特征向量组成的正交基。等价地，$T$ 可对角化为
$$T = U \Lambda U^*$$
其中 $U$ 是酉矩阵，$\Lambda$ 是对角矩阵。

**实空间版本**：自伴算子可正交对角化。

---

## 六、广义特征向量

### 6.1 定义

**定义**：向量 $v$ 称为 $T$ 的**广义特征向量**（属于特征值 $\lambda$），若存在正整数 $k$ 使得
$$(T - \lambda I)^k v = 0$$

**广义特征子空间**：$G_\lambda = \bigcup_{k=1}^\infty \ker(T - \lambda I)^k$

### 6.2 广义特征子空间的性质

**定理**：
1. $V = G_{\lambda_1} \oplus \cdots \oplus G_{\lambda_k}$（直和分解）
2. 每个 $G_\lambda$ 都是 $T$ 的不变子空间
3. $\dim G_\lambda$ 等于 $\lambda$ 在特征多项式中的重数

### 6.3 与Jordan标准形的关系

广义特征子空间的基给出Jordan标准形的构造：
- $G_\lambda$ 的维数 = $\lambda$ 的代数重数
- $G_\lambda$ 中Jordan链的数目 = $\lambda$ 的几何重数

---

## 重要定理汇总

| 定理 | 内容 |
|------|------|
| Cayley-Hamilton | $\chi_T(T) = 0$ |
| 极小多项式性质 | $m_T \mid \chi_T$，同不可约因子 |
| 谱定理 | 正规算子可酉对角化 |
| 广义特征分解 | $V = \bigoplus G_\lambda$ |

## 总结

- 不变子空间是理解线性算子结构的入口，也直接影响矩阵的分块表示。
- 极小多项式和 Cayley-Hamilton 定理为算子计算提供了统一代数工具。
- 正规算子、谱定理和广义特征分解共同刻画了有限维线性算子的深层结构。

## 易错点

- 把不变子空间和任意子空间混淆，忘记检验 $T(W) \subseteq W$。
- 把 Cayley-Hamilton 定理误写成特征多项式等于零，而不是代入算子后为零。
- 混淆正规、自伴和酉算子的条件。
- 把广义特征向量和特征向量视为同一概念。

## 练习（分层）

### A档（基础）

1. 判断一个子空间是否是不变子空间。
2. 写出一个线性变换在不变子空间分解下的分块矩阵。
3. 计算一个简单矩阵的特征多项式。
4. 判断一个算子是否满足 Cayley-Hamilton 形式。

### B档（进阶）

1. 求一个线性算子的极小多项式。
2. 证明某个子空间是特征子空间。
3. 验证一个算子是否正规或自伴。
4. 用广义特征子空间分解一个例子。

### C档（挑战）

1. 证明 Cayley-Hamilton 定理的一个常见证明思路。
2. 研究极小多项式与 Jordan 标准形的关系。
3. 证明谱定理的有限维版本。
4. 构造一个不能直接对角化但能 Jordan 分解的线性算子。

---

## 相关链接

- [[13_Eigenvalues|特征值与特征向量]] - 基础概念
- [[16_Jordan_Canonical|Jordan标准形]] - 广义特征向量的应用
- [[09_特殊矩阵类]] - 正规矩阵、自伴矩阵

---

**创建时间**：2026年4月3日
**所属模块**：高等代数 → 线性变换深化
