# 2026.3.29 Coding 记录



## 今日主题：图论基础——最短路径算法

### 学习内容总结

**核心知识点：**
- Dijkstra算法：带权图单源最短路径，贪心+优先队列
- Floyd-Warshall算法：全源最短路径，动态规划
- Bellman-Ford算法：可处理负权边，松弛操作
- 关键路径：DAG中的最长路径，拓扑排序应用

**算法对比：**

| 维度 | Dijkstra | Floyd-Warshall | Bellman-Ford |
|------|----------|----------------|--------------|
| 适用图 | 带权非负图 | 任意带权图 | 可含负权边 |
| 时间复杂度 | O((V+E)logV) | O(V³) | O(VE) |
| 空间复杂度 | O(V) | O(V²) | O(V) |
| 是否单源 | 是 | 否（全源） | 是 |

---

## 今日LeetCode题目

### LeetCode 743 - 网络延迟时间

**题目描述：**
- 给定有向带权图，求信号从源节点传播到所有节点的最短时间
- 输入：times数组，每个元素[u, v, w]表示从u到v需要w时间
- 输出：信号传播到所有节点的最短时间，无法传播到所有节点则返回-1

**建模思路：**
- 单源最短路径问题，适合Dijkstra算法
- 节点编号1..n，边权为正，可使用优先队列优化
- 最终答案为所有最短路径的最大值

---

## 方法1：Dijkstra算法（优先队列）

```cpp
#include <vector>
#include <queue>
#include <climits>
using namespace std;

class Solution {
public:
    int networkDelayTime(vector<vector<int>>& times, int n, int k) {
        // 构建邻接表
        vector<vector<pair<int, int>>> graph(n + 1);
        for (auto& t : times) {
            graph[t[0]].push_back({t[1], t[2]});
        }
        
        // 距离数组
        vector<int> dist(n + 1, INT_MAX);
        dist[k] = 0;
        
        // 优先队列（最小堆）
        priority_queue<pair<int, int>, vector<pair<int, int>>>, greater<pair<int, int>>> pq;
        pq.push({0, k});
        
        while (!pq.empty()) {
            auto [d, u] = pq.top();
            pq.pop();
            
            if (d > dist[u]) continue;
            
            for (auto& [v, w] : graph[u]) {
                if (dist[u] + w < dist[v]) {
                    dist[v] = dist[u] + w;
                    pq.push({dist[v], v});
                }
            }
        }
        
        // 求最大距离
        int maxDist = 0;
        for (int i = 1; i <= n; i++) {
            if (dist[i] == INT_MAX) return -1;
            maxDist = max(maxDist, dist[i]);
        }
        return maxDist;
    }
};
```

**复杂度：**
- 时间复杂度：O((V+E)logV)
- 空间复杂度：O(V+E)

---

## 今日总结

- **学习重点：** 理解Dijkstra算法的贪心思想与优先队列实现
- **核心概念：** 单源最短路径、松弛操作、优先队列优化
- **必做任务：** 完成LeetCode 743（网络延迟时间）✅
- **进阶探索：** Floyd-Warshall全源算法、Bellman-Ford负权处理

**关键洞察：**
- Dijkstra算法要求非负权边，贪心选择当前最短路径
- 优先队列将时间复杂度从O(V²)优化到O((V+E)logV)
- 关键路径问题可转化为最长路径（DAG拓扑排序）
---
[[## 明日预告

**3月30日：图论最小生成树**
- 核心题目：LeetCode 1584（连接所有点的最小费用）
- Prim算法与Kruskal算法对比
- 并查集在Kruskal算法中的应用



## 今日LeetCode提交

> ⚠️ LeetCode账号未登录，无法获取提交记录
> 
> 请手动记录今日提交情况


---

## 今日总结

- **学习重点：** 理解Dijkstra算法的贪心思想与优先队列实现
- **核心概念：** 
- **必做任务：** 
- **进阶探索：** 

**关键洞察：**
- 

---

## 明日预告

**3月30日：图论最小生成树**
- 核心题目：
- 知识点：

---

## 知识图谱连接

- [[图论基础]] ← 包含 → [[今日主题]]
- [[今日主题]] → 算法 → [[方法1]]
---
[[**零压力底线达成：** ✅ 理解基本思想，掌握核心算法
