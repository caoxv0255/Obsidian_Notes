---
type: chapter
subject: linear_algebra
chapter: 01
created: 2026-04-04
updated: 2026-04-04
status: complete
---

# 01 向量空间与线性变换

> 线性代数的核心基础：从向量空间公理到基变换

## 章节导引

向量空间是线性代数的基本研究对象。本章从向量空间的公理定义出发，逐步建立线性相关性、基与维数、坐标变换等核心概念，为后续的矩阵理论和线性方程组奠定基础。

**核心问题**：
- 什么是向量空间？
- 如何判断向量组的线性相关性？
- 什么是基和维数？如何进行基变换？

**学习目标**：
- 掌握向量空间的公理定义和常见例子
- 理解线性相关/无关的概念和判定方法
- 掌握基、维数、坐标的概念和计算
- 理解基变换和坐标变换

---

## 1. 向量空间

### 1.1 定义与公理

**定义**：向量空间 $V$ 是一个集合，配备了两个运算：
- **加法**：$V \times V \to V$，记作 $\mathbf{u} + \mathbf{v}$
- **标量乘法**：$\mathbb{F} \times V \to V$，记作 $\lambda\mathbf{v}$

**向量空间八条公理**：

**加法公理**：
1. 封闭性：$\forall \mathbf{u}, \mathbf{v} \in V, \mathbf{u} + \mathbf{v} \in V$
2. 结合律：$(\mathbf{u} + \mathbf{v}) + \mathbf{w} = \mathbf{u} + (\mathbf{v} + \mathbf{w})$
3. 交换律：$\mathbf{u} + \mathbf{v} = \mathbf{v} + \mathbf{u}$
4. 零元素：$\exists \mathbf{0} \in V, \forall \mathbf{v} \in V, \mathbf{v} + \mathbf{0} = \mathbf{v}$
5. 逆元素：$\forall \mathbf{v} \in V, \exists -\mathbf{v} \in V, \mathbf{v} + (-\mathbf{v}) = \mathbf{0}$

**标量乘法公理**：
6. 封闭性：$\forall \lambda \in \mathbb{F}, \mathbf{v} \in V, \lambda\mathbf{v} \in V$
7. 结合律：$\lambda(\mu\mathbf{v}) = (\lambda\mu)\mathbf{v}$
8. 分配律：$(\lambda + \mu)\mathbf{v} = \lambda\mathbf{v} + \mu\mathbf{v}$，$\lambda(\mathbf{u} + \mathbf{v}) = \lambda\mathbf{u} + \lambda\mathbf{v}$
9. 单位元：$1 \cdot \mathbf{v} = \mathbf{v}$

### 1.2 常见向量空间

| 向量空间 | 描述 | 维数 |
|---------|------|------|
| $\mathbb{R}^n$ | $n$ 维实向量 | $n$ |
| $\mathbb{C}^n$ | $n$ 维复向量 | $n$ |
| $P_n$ | 次数不超过 $n$ 的多项式 | $n+1$ |
| $M_{m \times n}$ | $m \times n$ 矩阵 | $m \times n$ |
| $C[a,b]$ | 连续函数空间 | 无限维 |

### 1.3 向量的三种观点

**观点 1：箭头（几何观点）**
- 向量是从原点出发的有向线段
- 长度表示范数，方向表示向量方向
- 直观理解向量加法和标量乘法

**观点 2：数字列表（计算观点）**
- $\mathbf{v} = [v_1, v_2, \ldots, v_n]^T$
- 适合计算机存储和数值计算
- 机器学习中作为特征向量

**观点 3：抽象对象（数学观点）**
- 关注运算规则而非具体表示
- 适合理论证明和推广
- 理解函数空间等抽象空间

---

## 2. 向量运算

### 2.1 基本运算

**向量加法**：
$$\mathbf{u} + \mathbf{v} = [u_1 + v_1, u_2 + v_2, \ldots, u_n + v_n]^T$$

**标量乘法**：
$$\lambda\mathbf{v} = [\lambda v_1, \lambda v_2, \ldots, \lambda v_n]^T$$

### 2.2 内积（点积）

**代数定义**：
$$\mathbf{u} \cdot \mathbf{v} = \sum_{i=1}^n u_i v_i$$

**几何定义**：
$$\mathbf{u} \cdot \mathbf{v} = \|\mathbf{u}\| \|\mathbf{v}\| \cos\theta$$

**重要性质**：
- 对称性：$\mathbf{u} \cdot \mathbf{v} = \mathbf{v} \cdot \mathbf{u}$
- 线性性：$(\mathbf{u} + \mathbf{w}) \cdot \mathbf{v} = \mathbf{u} \cdot \mathbf{v} + \mathbf{w} \cdot \mathbf{v}$
- 正定性：$\mathbf{v} \cdot \mathbf{v} \geq 0$，等号成立当且仅当 $\mathbf{v} = \mathbf{0}$

**应用**：
- 计算夹角：$\cos\theta = \frac{\mathbf{u} \cdot \mathbf{v}}{\|\mathbf{u}\| \|\mathbf{v}\|}$
- 计算投影：$\text{proj}_{\mathbf{v}}(\mathbf{u}) = \frac{\mathbf{u} \cdot \mathbf{v}}{\mathbf{v} \cdot \mathbf{v}} \mathbf{v}$
- 判断正交：$\mathbf{u} \cdot \mathbf{v} = 0 \iff \mathbf{u} \perp \mathbf{v}$

### 2.3 向量范数

**L2 范数（欧几里得范数）**：
$$\|\mathbf{v}\|_2 = \sqrt{v_1^2 + v_2^2 + \cdots + v_n^2}$$

**L1 范数（曼哈顿范数）**：
$$\|\mathbf{v}\|_1 = |v_1| + |v_2| + \cdots + |v_n|$$

**L∞ 范数（最大范数）**：
$$\|\mathbf{v}\|_\infty = \max\{|v_1|, |v_2|, \ldots, |v_n|\}$$

---

## 3. 线性相关性

### 3.1 核心定义

**线性组合**：
$$\mathbf{b} = c_1\mathbf{v}_1 + c_2\mathbf{v}_2 + \cdots + c_n\mathbf{v}_n$$

**线性相关**：存在不全为零的 $c_1, \ldots, c_n$ 使得
$$c_1\mathbf{v}_1 + c_2\mathbf{v}_2 + \cdots + c_n\mathbf{v}_n = \mathbf{0}$$

**线性无关**：只有 $c_1 = c_2 = \cdots = c_n = 0$ 满足上式

### 3.2 判定定理

| 方法 | 条件 | 适用场景 |
|-----|------|---------|
| 定义法 | 存在非零解使组合为零 | 理论证明 |
| 行列式法 | $\det(A) = 0$ | $n$ 个 $n$ 维向量 |
| 秩法 | $\text{rank}(A) < n$ | 一般情况 |

**重要结论**：
- $n+1$ 个 $n$ 维向量必线性相关
- 包含零向量的向量组必线性相关
- 正交非零向量组必线性无关

### 3.3 几何意义

| 维度 | 线性相关 | 线性无关 |
|-----|---------|---------|
| 2D | 共线 | 不共线 |
| 3D | 共面 | 不共面 |
| nD | 落在低维空间 | 张成全空间 |

---

## 4. 基与维数

### 4.1 基的定义

**基**：向量组 $\mathcal{B} = \{\mathbf{v}_1, \mathbf{v}_2, \ldots, \mathbf{v}_n\}$ 是 $V$ 的基，当且仅当：
1. **线性无关**：基中的向量线性无关
2. **生成 $V$**：$V$ 中任意向量都可由基线性表示

**维数**：$\dim(V)$ = 基中向量的个数

### 4.2 唯一表示定理

**定理**：如果 $\mathcal{B}$ 是 $V$ 的基，则任意 $\mathbf{v} \in V$ 可以**唯一**表示为：
$$\mathbf{v} = c_1 \mathbf{v}_1 + c_2 \mathbf{v}_2 + \cdots + c_n \mathbf{v}_n$$

**坐标**：$[\mathbf{v}]_{\mathcal{B}} = [c_1, c_2, \ldots, c_n]^T$

### 4.3 常见基

**标准基**：$\mathcal{E} = \{\mathbf{e}_1, \mathbf{e}_2, \ldots, \mathbf{e}_n\}$

$$\mathbf{e}_i = [0, \ldots, 0, 1, 0, \ldots, 0]^T$$

**正交基**：$\mathbf{v}_i \cdot \mathbf{v}_j = 0$ $(i \neq j)$

**标准正交基**：$\mathbf{v}_i \cdot \mathbf{v}_j = \delta_{ij}$

### 4.4 维数公式

**定理**：对于子空间 $W_1, W_2$：
$$\dim(W_1 + W_2) = \dim(W_1) + \dim(W_2) - \dim(W_1 \cap W_2)$$

---

## 5. 基变换

### 5.1 坐标变换

设 $\mathcal{B}$ 和 $\mathcal{C}$ 是 $V$ 的两个基，基变换矩阵 $P_{\mathcal{C} \leftarrow \mathcal{B}}$ 满足：
$$[\mathbf{v}]_{\mathcal{C}} = P_{\mathcal{C} \leftarrow \mathcal{B}} [\mathbf{v}]_{\mathcal{B}}$$

### 5.2 计算方法

基变换矩阵的列是 $\mathcal{B}$ 中的向量在 $\mathcal{C}$ 下的坐标：
$$P_{\mathcal{C} \leftarrow \mathcal{B}} = [\mathbf{b}_1]_{\mathcal{C}} \quad [\mathbf{b}_2]_{\mathcal{C}} \quad \cdots \quad [\mathbf{b}_n]_{\mathcal{C}}$$

### 5.3 性质

1. **可逆性**：$P_{\mathcal{C} \leftarrow \mathcal{B}}^{-1} = P_{\mathcal{B} \leftarrow \mathcal{C}}$
2. **链式法则**：$P_{\mathcal{D} \leftarrow \mathcal{B}} = P_{\mathcal{D} \leftarrow \mathcal{C}} \cdot P_{\mathcal{C} \leftarrow \mathcal{B}}$

---

## 6. 线性变换

### 6.1 定义

**线性变换** $T: V \to W$ 满足：
1. $T(\mathbf{u} + \mathbf{v}) = T(\mathbf{u}) + T(\mathbf{v})$
2. $T(c\mathbf{v}) = cT(\mathbf{v})$

### 6.2 矩阵表示

给定基 $\mathcal{B}$ 和 $\mathcal{C}$，线性变换 $T$ 的矩阵表示 $[T]_{\mathcal{C} \leftarrow \mathcal{B}}$ 满足：
$$[T(\mathbf{v})]_{\mathcal{C}} = [T]_{\mathcal{C} \leftarrow \mathcal{B}} [\mathbf{v}]_{\mathcal{B}}$$

### 6.3 常见线性变换

| 变换 | 矩阵 | 效果 |
|-----|------|------|
| 旋转 | $\begin{bmatrix} \cos\theta & -\sin\theta \\ \sin\theta & \cos\theta \end{bmatrix}$ | 旋转 $\theta$ 角 |
| 缩放 | $\begin{bmatrix} s_x & 0 \\ 0 & s_y \end{bmatrix}$ | 各方向伸缩 |
| 投影 | $\begin{bmatrix} 1 & 0 \\ 0 & 0 \end{bmatrix}$ | 投影到 $x$ 轴 |
| 反射 | $\begin{bmatrix} 1 & 0 \\ 0 & -1 \end{bmatrix}$ | 关于 $x$ 轴对称 |

---

## 7. 代码实现

### 7.1 向量运算

```python
import numpy as np

# 创建向量
v1 = np.array([1, 2, 3])
v2 = np.array([4, 5, 6])

# 向量加法
v_sum = v1 + v2

# 标量乘法
v_scaled = 2 * v1

# 点积
dot_product = np.dot(v1, v2)

# 范数
norm_l2 = np.linalg.norm(v1)
norm_l1 = np.linalg.norm(v1, ord=1)
norm_inf = np.linalg.norm(v1, ord=np.inf)

# 向量夹角
cos_theta = np.dot(v1, v2) / (np.linalg.norm(v1) * np.linalg.norm(v2))
theta = np.arccos(cos_theta)

# 向量投影
def projection(u, v):
    return (np.dot(u, v) / np.dot(v, v)) * v

proj = projection(v1, v2)
```

### 7.2 线性相关性判断

```python
def check_linear_independence(vectors):
    """判断向量组是否线性无关"""
    A = np.column_stack(vectors)
    rank = np.linalg.matrix_rank(A)
    n_vectors = len(vectors)
    return rank == n_vectors, rank

# 示例
v1 = np.array([1, 0, 0])
v2 = np.array([0, 1, 0])
v3 = np.array([0, 0, 1])

is_independent, rank = check_linear_independence([v1, v2, v3])
print(f"线性无关: {is_independent}, 秩: {rank}")
```

### 7.3 Gram-Schmidt 正交化

```python
def gram_schmidt(vectors, normalize=True):
    """Gram-Schmidt 正交化"""
    vectors = np.array(vectors, dtype=float)
    n = vectors.shape[1]
    orthogonal = []
    
    for i in range(n):
        v = vectors[:, i].copy()
        for u in orthogonal:
            v = v - np.dot(v, u) / np.dot(u, u) * u
        
        if np.linalg.norm(v) > 1e-10:
            if normalize:
                v = v / np.linalg.norm(v)
            orthogonal.append(v)
    
    return np.column_stack(orthogonal)

# 示例
vectors = np.array([[1, 1, 1], [1, 0, 1], [0, 1, 1]]).T
orthogonal_basis = gram_schmidt(vectors)
print("正交基:\n", orthogonal_basis)
```

### 7.4 基变换

```python
def basis_change_matrix(B_old, B_new):
    """计算基变换矩阵"""
    return np.linalg.solve(B_new, B_old)

def coordinates_in_basis(vector, basis):
    """计算向量在基下的坐标"""
    return np.linalg.solve(basis, vector)

# 示例
E = np.array([[1, 0, 0], [0, 1, 0], [0, 0, 1]]).T  # 标准基
B = np.array([[1, 1, 0], [1, 0, 1], [0, 1, 1]]).T  # 新基

P = basis_change_matrix(E, B)
v = np.array([3, 4, 5])
coords = coordinates_in_basis(v, B)
print(f"坐标: {coords}")
```

---

## 8. 重要不等式

### 8.1 柯西-施瓦茨不等式

$$|\mathbf{u} \cdot \mathbf{v}| \leq \|\mathbf{u}\| \|\mathbf{v}\|$$

等号成立当且仅当 $\mathbf{u}$ 和 $\mathbf{v}$ 线性相关。

### 8.2 三角不等式

$$\|\mathbf{u} + \mathbf{v}\| \leq \|\mathbf{u}\| + \|\mathbf{v}\|$$

### 8.3 平行四边形定律

$$\|\mathbf{u} + \mathbf{v}\|^2 + \|\mathbf{u} - \mathbf{v}\|^2 = 2\|\mathbf{u}\|^2 + 2\|\mathbf{v}\|^2$$

---

## 9. 例题精选

### 例题 1：判断线性相关性

**问题**：判断 $\mathbf{v}_1 = [1, 2, 3]^T$，$\mathbf{v}_2 = [4, 5, 6]^T$，$\mathbf{v}_3 = [7, 8, 9]^T$ 是否线性相关。

**解**：构造矩阵并求行列式：
$$\det\begin{bmatrix} 1 & 4 & 7 \\ 2 & 5 & 8 \\ 3 & 6 & 9 \end{bmatrix} = 0$$

因此线性相关。

### 例题 2：求坐标

**问题**：求 $\mathbf{v} = [5, 7]^T$ 在基 $\mathcal{B} = \{[1, 1]^T, [1, -1]^T\}$ 下的坐标。

**解**：设 $\mathbf{v} = c_1[1, 1]^T + c_2[1, -1]^T$

解方程组：
$$\begin{cases} c_1 + c_2 = 5 \\ c_1 - c_2 = 7 \end{cases}$$

得 $c_1 = 6, c_2 = -1$，即 $[\mathbf{v}]_{\mathcal{B}} = [6, -1]^T$。

### 例题 3：基变换

**问题**：设 $\mathcal{B} = \{[1, 1, 0]^T, [1, 0, 1]^T, [0, 1, 1]^T\}$，求基变换矩阵 $P_{\mathcal{E} \leftarrow \mathcal{B}}$（$\mathcal{E}$ 是标准基）。

**解**：由于 $\mathcal{E}$ 是标准基，基变换矩阵的列就是 $\mathcal{B}$ 中的向量：
$$P_{\mathcal{E} \leftarrow \mathcal{B}} = \begin{bmatrix} 1 & 1 & 0 \\ 1 & 0 & 1 \\ 0 & 1 & 1 \end{bmatrix}$$

---

## 10. 习题

### 基础题

1. 验证 $\mathbb{R}^3$ 是向量空间。
2. 计算向量 $\mathbf{v} = [3, 4]$ 的 L1、L2、L∞ 范数。
3. 判断向量组 $\{[1, 1]^T, [2, 2]^T\}$ 是否线性相关。
4. 求 $\mathbf{v} = [1, 2, 3]^T$ 在基 $\{[1, 0, 0]^T, [0, 1, 0]^T, [0, 0, 1]^T\}$ 下的坐标。
5. 对向量组 $\{[1, 1]^T, [1, 0]^T\}$ 进行 Gram-Schmidt 正交化。

### 进阶题

6. 证明：零向量的唯一性。
7. 证明：$n+1$ 个 $n$ 维向量必线性相关。
8. 证明：正交非零向量组线性无关。
9. 设 $W_1 = \{(x, y, z) \mid x + y + z = 0\}$，求 $\dim(W_1)$。
10. 证明维数公式。

### 挑战题

11. 研究无限维向量空间的基（Hamel 基）。
12. 证明：任何线性无关组都可扩展为基。
13. 研究函数空间 $C[0, 1]$ 的内积定义。
14. 证明 Gram-Schmidt 过程的正确性。
15. 分析不同范数之间的关系。

---

## 11. 知识关联

### 本章 → 后续章节

```
向量空间 → 矩阵运算（矩阵作为线性变换）
线性相关性 → 线性方程组（解的结构）
基与维数 → 特征值（特征向量构成基）
基变换 → 相似对角化（基变换下的矩阵）
```

### 数学 → 专业应用

**量化金融**：
- 向量空间 → 资产空间、组合空间
- 线性相关性 → 因子正交化
- 基变换 → 风险因子分解

**计算神经科学**：
- 向量空间 → 神经状态空间
- 线性相关性 → 神经编码效率
- 基变换 → 神经表征分析

---

## 12. 参考资源

### 教材
- 《线性代数应该这样学》- Sheldon Axler
- 《线性代数导论》- Gilbert Strang
- 《线性代数的几何意义》- 任广千

### 视频课程
- MIT 18.06 - Gilbert Strang
- 3Blue1Brown 线性代数的本质

### 练习
- 教材课后习题
- MIT 习题集

---

**下一章**：[[02_矩阵运算|矩阵运算 →]]

**返回索引**：[[00_Linear_Algebra_Index|线性代数索引]]
