---
type: note
subject: advanced_algebra
chapter: 08
created: 2026-04-03
updated: 2026-04-24
status: complete
---

# 矩阵微积分

## 📌 学习目标

- 理解矩阵求导、梯度和 Hessian 的基本规则
- 掌握矩阵微积分在极值判定与机器学习中的应用
- 能将矩阵指数和自动微分公式用于实际优化问题

## ✅ 先修

- [[07_矩阵函数]]
- [[06_矩阵范数]]
- [[../Linear/23_Linear_Maps|线性映射]]
- [[../00_Symbols_Conventions|符号与约定总表]]

## 难度分层

- **基础**：矩阵导数、梯度、Hessian
- **进阶**：矩阵函数求导、链式法则、极值判定
- **拓展**：机器学习优化、反向传播、自动微分

## 自测（3问速测）

1. 为什么矩阵导数通常要保持维数一致？
2. Hessian 矩阵为什么和二阶极值判定有关？
3. 反向传播为什么可以看作链式法则的重复应用？

## 一、矩阵函数的导数

### 1.1 标量对矩阵的导数

**定义**：设 $f: \mathbb{R}^{m \times n} \to \mathbb{R}$，定义 $f$ 对矩阵 $X$ 的导数为
$$\frac{\partial f}{\partial X} = \left(\frac{\partial f}{\partial X_{ij}}\right)_{m \times n}$$

即 $(i,j)$ 位置元素是 $f$ 对 $X_{ij}$ 的偏导数。

### 1.2 基本例子

**线性函数**：$f(X) = \text{tr}(AX)$
$$\frac{\partial}{\partial X} \text{tr}(AX) = A^T$$

**二次型**：$f(X) = x^T A x$
$$\frac{\partial}{\partial x} (x^T A x) = (A + A^T)x$$

若 $A$ 对称：$\frac{\partial}{\partial x} (x^T A x) = 2Ax$

---

## 二、迹的导数

### 2.1 基本公式

| 函数 $f(X)$ | 导数 $\frac{\partial f}{\partial X}$ |
|-------------|--------------------------------------|
| $\text{tr}(AX)$ | $A^T$ |
| $\text{tr}(X^T A)$ | $A$ |
| $\text{tr}(X^T X)$ | $2X$ |
| $\text{tr}(X X^T)$ | $2X$ |
| $\text{tr}(AX^T B)$ | $BA$ |
| $\text{tr}(AXB)$ | $A^T B^T$ |

### 2.2 证明技巧

**利用微分**：$df = \text{tr}(G^T dX) \Rightarrow \frac{\partial f}{\partial X} = G$

**例**：$f(X) = \text{tr}(AX^T B)$
$$df = \text{tr}(A(dX^T)B) = \text{tr}(B A dX^T) = \text{tr}((BA)^T dX)$$
故 $\frac{\partial f}{\partial X} = (BA)^T = A^T B^T$，再由迹的性质得 $BA$。

---

## 三、行列式的导数

### 3.1 基本公式

**定理**：设 $X$ 可逆，则
$$\frac{\partial \det(X)}{\partial X} = \det(X) \cdot (X^{-1})^T$$

**证明**：由 Jacobi公式，$d\det(X) = \det(X) \cdot \text{tr}(X^{-1} dX)$。

利用 $\text{tr}(X^{-1} dX) = \text{tr}((X^{-1})^T dX^T)^T = \text{tr}((X^{-1})^T dX)$（标量转置不变）。

### 3.2 对数行列式

**推论**：
$$\frac{\partial \log \det(X)}{\partial X} = (X^{-1})^T$$

### 3.3 行列式平方的导数

$$\frac{\partial \det(X)^2}{\partial X} = 2\det(X) \cdot (X^{-1})^T \det(X) = 2\det(X)^2 (X^{-1})^T$$

---

## 四、矩阵逆的导数

### 4.1 公式

**定理**：
$$\frac{\partial X^{-1}}{\partial X_{ij}} = -X^{-1} E_{ij} X^{-1}$$

其中 $E_{ij}$ 是 $(i,j)$ 位置为1其余为0的矩阵。

**矩阵形式**：$d(X^{-1}) = -X^{-1} (dX) X^{-1}$

### 4.2 应用

**例**：求 $\frac{\partial \text{tr}(A X^{-1} B)}{\partial X}$

**解**：
$$d\,\text{tr}(AX^{-1}B) = \text{tr}(A \cdot dX^{-1} \cdot B) = -\text{tr}(AX^{-1} dX X^{-1} B)$$
$$= -\text{tr}(X^{-1} B A X^{-1} dX)$$

故
$$\frac{\partial \text{tr}(A X^{-1} B)}{\partial X} = -(X^{-1} B A X^{-1})^T$$

---

## 五、Jacobian矩阵

### 5.1 定义

**定义**：设 $f: \mathbb{R}^n \to \mathbb{R}^m$，$f(x) = (f_1(x), \ldots, f_m(x))^T$，**Jacobian矩阵**定义为
$$J_f(x) = \begin{pmatrix} \frac{\partial f_1}{\partial x_1} & \cdots & \frac{\partial f_1}{\partial x_n} \\ \vdots & \ddots & \vdots \\ \frac{\partial f_m}{\partial x_1} & \cdots & \frac{\partial f_m}{\partial x_n} \end{pmatrix}$$

### 5.2 性质

1. **链式法则**：$J_{f \circ g}(x) = J_f(g(x)) \cdot J_g(x)$

2. **线性映射**：若 $f(x) = Ax$，则 $J_f = A$

3. **逆函数定理**：若 $J_f$ 在某点可逆，则 $f$ 在该点附近有逆

### 5.3 例子

**例**：$f: \mathbb{R}^2 \to \mathbb{R}^2$，$f(x,y) = (x^2 + y^2, xy)$

$$J_f = \begin{pmatrix} 2x & 2y \\ y & x \end{pmatrix}$$

---

## 六、Hessian矩阵

### 6.1 定义

**定义**：设 $f: \mathbb{R}^n \to \mathbb{R}$ 二阶可微，**Hessian矩阵**定义为
$$H_f(x) = \begin{pmatrix} \frac{\partial^2 f}{\partial x_1^2} & \cdots & \frac{\partial^2 f}{\partial x_1 \partial x_n} \\ \vdots & \ddots & \vdots \\ \frac{\partial^2 f}{\partial x_n \partial x_1} & \cdots & \frac{\partial^2 f}{\partial x_n^2} \end{pmatrix}$$

### 6.2 性质

1. **对称性**：若 $f$ 二阶连续可微，则 $H_f$ 对称

2. **与梯度的关系**：$H_f = J_{\nabla f}$

3. **Taylor展开**：
$$f(x + h) = f(x) + \nabla f(x)^T h + \frac{1}{2} h^T H_f(x) h + o(\|h\|^2)$$

### 6.3 极值判定

设 $\nabla f(x_0) = 0$，则：

| 条件 | 结论 |
|------|------|
| $H_f(x_0)$ 正定 | 严格局部极小 |
| $H_f(x_0)$ 负定 | 严格局部极大 |
| $H_f(x_0)$ 不定 | 鞍点 |
| $H_f(x_0)$ 半定 | 需要更高阶信息 |

---

## 七、矩阵函数的导数

### 7.1 标量参数

**定义**：设 $A(t)$ 是参数 $t$ 的矩阵函数，定义
$$\frac{dA}{dt} = \left(\frac{dA_{ij}}{dt}\right)$$

**基本公式**：
- $\frac{d}{dt}(A + B) = \dot{A} + \dot{B}$
- $\frac{d}{dt}(AB) = \dot{A}B + A\dot{B}$
- $\frac{d}{dt}(A^{-1}) = -A^{-1} \dot{A} A^{-1}$

### 7.2 矩阵指数的导数

**公式**：
$$\frac{d}{dt} e^{tA} = A e^{tA} = e^{tA} A$$

更一般地：
$$\frac{d}{dt} e^{A(t)} \neq A'(t) e^{A(t)}$$（当 $A(t)$ 与 $A'(t)$ 不交换时）

**正确公式**：
$$\frac{d}{dt} e^{A(t)} = \int_0^1 e^{sA(t)} A'(t) e^{(1-s)A(t)} ds$$

---

## 八、应用：机器学习中的优化

### 8.1 线性回归

**问题**：$\min_w \|Xw - y\|^2$

**解**：$\nabla_w \|Xw - y\|^2 = 2X^T(Xw - y) = 0$

正规方程：$X^T X w = X^T y$

### 8.2 Logistic回归

**损失函数**：$L(w) = -\sum_i [y_i \log \sigma(x_i^T w) + (1-y_i) \log(1 - \sigma(x_i^T w))]$

**梯度**：
$$\nabla_w L = \sum_i (\sigma(x_i^T w) - y_i) x_i$$

**Hessian**：
$$H_L = X^T D X$$
其中 $D = \text{diag}(\sigma(x_i^T w)(1 - \sigma(x_i^T w)))$

### 8.3 神经网络的反向传播

**关键公式**：设 $z = Wx + b$，$\hat{y} = \sigma(z)$，损失 $L$，则

$$\frac{\partial L}{\partial W} = \frac{\partial L}{\partial z} x^T$$
$$\frac{\partial L}{\partial x} = W^T \frac{\partial L}{\partial z}$$

---

## 重要公式汇总

| 函数 | 导数 |
|------|------|
| $\text{tr}(AX)$ | $A^T$ |
| $\det(X)$ | $\det(X) (X^{-1})^T$ |
| $\log \det(X)$ | $(X^{-1})^T$ |
| $X^{-1}$ | $-X^{-1} \otimes X^{-1}$（按元素） |
| $\|X\|_F^2$ | $2X$ |

---

## 总结

- 矩阵微积分把偏导、梯度、Hessian 和矩阵求导统一成可操作的公式体系。
- 二阶信息决定了局部极值性质，而 Hessian 是最核心的判据对象。
- 在机器学习中，矩阵微积分是损失函数优化与反向传播的数学基础。

## 易错点

- 混淆梯度的行列形状。
- 忘记矩阵求导的转置约定。
- 把 Hessian 只当成“二阶导数矩阵”而忽略对称性和局部极值含义。
- 在链式法则中漏掉中间变量。

## 练习（分层）

### A档（基础）

1. 计算 $\text{tr}(AX)$ 的矩阵导数。
2. 求一个二次型的梯度。
3. 写出一个函数的 Hessian。
4. 判断一个驻点是极小、极大还是鞍点。

### B档（进阶）

1. 计算矩阵逆函数的导数。
2. 推导矩阵指数的导数公式。
3. 用 Hessian 分析一个优化问题。
4. 计算 Logistic 回归的梯度或 Hessian。

### C档（挑战）

1. 推导矩阵指数一般求导公式。
2. 分析反向传播的矩阵链式结构。
3. 证明一个二阶极值判别定理。
4. 用矩阵微积分重写一个简单神经网络的训练步骤。

## 相关链接

- [[13_Eigenvalues|特征值]] - Hessian与极值
- [[../../Optimization/01_Gradient_Descent|梯度下降]] - 优化算法
- [[../../02_programming/04_python/02_Autograd|自动微分]] - PyTorch实现

---

**创建时间**：2026年4月3日
**所属模块**：高等代数 → 矩阵分析
