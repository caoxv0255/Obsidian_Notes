---
type: chapter
subject: linear_algebra
chapter: 06
created: 2026-04-04
updated: 2026-04-04
status: complete
---

# 06 矩阵分解

> 将复杂矩阵分解为简单矩阵的乘积，揭示矩阵的内在结构

## 章节导引

矩阵分解是线性代数的核心工具，将复杂矩阵分解为具有特殊结构的矩阵乘积，不仅简化计算，更揭示矩阵的内在性质。本章系统讲解 LU、QR、Cholesky 等常用分解方法。

**核心问题**：
- 为什么要进行矩阵分解？
- 不同分解方法各有什么特点和应用？
- 如何选择合适的分解方法？

**学习目标**：
- 掌握 LU、QR、Cholesky 分解的原理
- 理解各分解方法的适用条件
- 能够应用分解方法解决实际问题

---

## 1. 矩阵分解概述

### 1.1 基本思想

将矩阵 $A$ 分解为若干简单矩阵的乘积：
$$A = BC \quad \text{或} \quad A = BCD$$

**优势**：
- 简化计算（如求解线性方程组）
- 揭示结构（如特征值、秩）
- 提高数值稳定性

### 1.2 常用分解方法

| 分解类型 | 形式 | 适用条件 | 主要应用 |
|---------|------|---------|---------|
| LU | $A = LU$ | 可逆方阵 | 线性方程组 |
| QR | $A = QR$ | 任意矩阵 | 最小二乘 |
| Cholesky | $A = LL^T$ | 对称正定 | 优化问题 |
| SVD | $A = U\Sigma V^T$ | 任意矩阵 | 降维、压缩 |

---

## 2. LU 分解

### 2.1 定义

对于方阵 $A$，若存在下三角矩阵 $L$ 和上三角矩阵 $U$ 使得：
$$A = LU$$

**完全形式**：$PA = LU$（带置换）

### 2.2 存在条件

- $A$ 可逆：LU 分解存在
- $A$ 的各阶顺序主子式非零：无需置换

### 2.3 计算方法

**Doolittle 算法**（$L$ 的对角元为 1）：

对于 $i = 1, 2, \ldots, n$：
- 计算 $U$ 的第 $i$ 行：$u_{ij} = a_{ij} - \sum_{k=1}^{i-1} l_{ik}u_{kj}$ $(j \geq i)$
- 计算 $L$ 的第 $i$ 列：$l_{ji} = \frac{1}{u_{ii}}(a_{ji} - \sum_{k=1}^{i-1} l_{jk}u_{ki})$ $(j > i)$

### 2.4 应用

**求解 $A\mathbf{x} = \mathbf{b}$**：
1. $L\mathbf{y} = \mathbf{b}$（前代）
2. $U\mathbf{x} = \mathbf{y}$（回代）

**优势**：多次求解不同 $\mathbf{b}$ 时，只需一次分解。

---

## 3. QR 分解

### 3.1 定义

对于 $m \times n$ 矩阵 $A$（$m \geq n$），存在：
$$A = QR$$

其中：
- $Q \in \mathbb{R}^{m \times n}$：列正交矩阵（$Q^T Q = I$）
- $R \in \mathbb{R}^{n \times n}$：上三角矩阵

### 3.2 计算方法

**Gram-Schmidt 正交化**：
1. 取 $A$ 的列向量 $\mathbf{a}_1, \ldots, \mathbf{a}_n$
2. 正交化得 $\mathbf{q}_1, \ldots, \mathbf{q}_n$
3. $Q = [\mathbf{q}_1, \ldots, \mathbf{q}_n]$
4. $R$ 由投影系数构成

**Householder 变换**（数值稳定）：
- 通过反射变换逐列将矩阵化为上三角

**Givens 旋转**：
- 通过旋转变换逐元素消零

### 3.3 应用

**最小二乘问题**：
$$\mathbf{x} = R^{-1}Q^T\mathbf{b}$$

**特征值计算**：QR 算法

---

## 4. Cholesky 分解

### 4.1 定义

对于对称正定矩阵 $A$，存在：
$$A = LL^T$$

其中 $L$ 是下三角矩阵。

### 4.2 存在条件

$A$ 对称正定 $\iff$ Cholesky 分解存在

### 4.3 计算方法

$$l_{ij} = \frac{1}{l_{jj}}\left(a_{ij} - \sum_{k=1}^{j-1} l_{ik}l_{jk}\right), \quad i > j$$

$$l_{jj} = \sqrt{a_{jj} - \sum_{k=1}^{j-1} l_{jk}^2}$$

### 4.4 应用

- 求解对称正定线性方程组
- Monte Carlo 模拟（生成相关随机变量）
- 优化算法（牛顿法）

---

## 5. 代码实现

### 5.1 LU 分解

```python
import numpy as np
from scipy.linalg import lu

A = np.array([[2, 1, 1], [4, 3, 3], [8, 7, 9]], dtype=float)

# SciPy LU 分解（带置换）
P, L, U = lu(A)

print(f"P =\n{P}")
print(f"L =\n{L}")
print(f"U =\n{U}")

# 验证
print(f"PA = LU: {np.allclose(P @ A, L @ U)}")

# 求解线性方程组
b = np.array([4, 10, 24], dtype=float)
Pb = P @ b
y = np.linalg.solve(L, Pb)  # 前代
x = np.linalg.solve(U, y)    # 回代
print(f"解 x = {x}")
print(f"验证 Ax = {A @ x}")
```

### 5.2 QR 分解

```python
import numpy as np
from scipy.linalg import qr

A = np.array([[1, 2], [3, 4], [5, 6]], dtype=float)

# QR 分解
Q, R = qr(A, mode='economic')

print(f"Q =\n{Q}")
print(f"R =\n{R}")

# 验证正交性
print(f"Q^T Q =\n{Q.T @ Q}")
print(f"正交: {np.allclose(Q.T @ Q, np.eye(2))}")

# 最小二乘
b = np.array([1, 2, 3], dtype=float)
x = np.linalg.solve(R, Q.T @ b)
print(f"最小二乘解: {x}")
```

### 5.3 Cholesky 分解

```python
import numpy as np

# 创建对称正定矩阵
A = np.array([[4, 2, 1], [2, 5, 3], [1, 3, 6]], dtype=float)

# Cholesky 分解
L = np.linalg.cholesky(A)

print(f"L =\n{L}")
print(f"LL^T =\n{L @ L.T}")
print(f"A = LL^T: {np.allclose(A, L @ L.T)}")

# 求解线性方程组
b = np.array([7, 10, 10], dtype=float)
y = np.linalg.solve(L, b)      # 前代
x = np.linalg.solve(L.T, y)    # 回代
print(f"解 x = {x}")
```

---

## 6. 分解方法比较

| 方法 | 计算复杂度 | 数值稳定性 | 存储需求 |
|-----|-----------|-----------|---------|
| LU | $O(n^3/3)$ | 中等（需选主元） | $n^2$ |
| QR (Householder) | $O(2mn^2/3)$ | 高 | $mn$ |
| Cholesky | $O(n^3/6)$ | 高 | $n^2/2$ |

**选择建议**：
- 一般线性方程组 → LU 分解
- 最小二乘问题 → QR 分解
- 对称正定矩阵 → Cholesky 分解
- 需要高数值稳定性 → QR 分解

---

## 7. 例题精选

### 例题 1：LU 分解

**问题**：对 $A = \begin{pmatrix} 2 & 1 \\ 4 & 3 \end{pmatrix}$ 进行 LU 分解

**解**：
$$L = \begin{pmatrix} 1 & 0 \\ 2 & 1 \end{pmatrix}, \quad U = \begin{pmatrix} 2 & 1 \\ 0 & 1 \end{pmatrix}$$

验证：$LU = \begin{pmatrix} 1 & 0 \\ 2 & 1 \end{pmatrix}\begin{pmatrix} 2 & 1 \\ 0 & 1 \end{pmatrix} = \begin{pmatrix} 2 & 1 \\ 4 & 3 \end{pmatrix} = A$

### 例题 2：QR 分解

**问题**：对 $A = \begin{pmatrix} 1 & 1 \\ 0 & 1 \end{pmatrix}$ 进行 QR 分解

**解**：Gram-Schmidt 正交化

$\mathbf{a}_1 = \begin{pmatrix} 1 \\ 0 \end{pmatrix}$, $\mathbf{a}_2 = \begin{pmatrix} 1 \\ 1 \end{pmatrix}$

$\mathbf{q}_1 = \begin{pmatrix} 1 \\ 0 \end{pmatrix}$

$\mathbf{v}_2 = \mathbf{a}_2 - (\mathbf{q}_1^T \mathbf{a}_2)\mathbf{q}_1 = \begin{pmatrix} 0 \\ 1 \end{pmatrix}$

$\mathbf{q}_2 = \begin{pmatrix} 0 \\ 1 \end{pmatrix}$

$$Q = \begin{pmatrix} 1 & 0 \\ 0 & 1 \end{pmatrix}, \quad R = \begin{pmatrix} 1 & 1 \\ 0 & 1 \end{pmatrix}$$

---

## 8. 习题

### 基础题

1. 对 $A = \begin{pmatrix} 3 & 1 \\ 6 & 4 \end{pmatrix}$ 进行 LU 分解
2. 用 QR 分解验证 $\begin{pmatrix} 1 & 1 \\ 1 & -1 \end{pmatrix}$ 的正交性
3. 判断 $A = \begin{pmatrix} 4 & 2 \\ 2 & 1 \end{pmatrix}$ 是否可进行 Cholesky 分解
4. 用 LU 分解求解 $\begin{cases} 2x + y = 5 \\ 4x + 3y = 11 \end{cases}$
5. 比较 LU 和 QR 分解的数值稳定性

### 进阶题

6. 实现 Householder 变换的 QR 分解
7. 分析选主元对 LU 分解稳定性的影响
8. 研究稀疏矩阵的 LU 分解
9. 实现块 Cholesky 分解算法
10. 研究 LDL^T 分解与 Cholesky 分解的关系

---

## 9. 知识关联

### 本章 → 后续章节

```
矩阵分解 → SVD（广义分解）
QR 分解 → 特征值计算
Cholesky → 优化算法
```

---

**上一章**：[[05_特征值与特征向量|特征值与特征向量 ←]]

**下一章**：[[07_奇异值分解|奇异值分解 →]]

**返回索引**：[[00_Linear_Algebra_Index|线性代数索引]]
